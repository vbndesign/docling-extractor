# Story 1.6: QA Hardening & Documentation

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [pytest, pytest-asyncio, ruff, coderabbit, coverage]
```

## Story

**As** the owner of the tool,
**I want** the v1 finalized with integration tests, corner case handling, and a complete README,
**so that** I can rely on the tool daily without surprises and can return to it in months without needing to re-learn how it works.

## Acceptance Criteria

1. Testes de integração cobrem explicitamente os seguintes corner cases, todos com expectativas bem definidas: (a) URL web que retorna 404, (b) URL web que retorna HTML vazio ou quase-vazio (SPA não-renderizada), (c) PDF sem texto (página totalmente imagem, simulando o caso que não será suportado por falta de OCR), (d) arquivo enviado que não é PDF válido, (e) upload excedendo `MAX_UPLOAD_MB`, (f) timeout na conversão.
2. Para o caso (c) PDF sem texto, o sistema retorna um erro claro orientando o usuário de que OCR não está habilitado na v1, em vez de produzir um arquivo vazio.
3. `README.md` é atualizado com seções: Visão geral, Pré-requisitos, Instalação, Como rodar, Como usar (passo a passo com screenshots opcionais; inclui menção ao Swagger UI automático em `http://127.0.0.1:8000/docs` para inspeção do endpoint `/extract` — S4), Troubleshooting (primeira execução baixando modelos, PDFs sem texto, URLs dinâmicas não suportadas), Estrutura do projeto, Limitações conhecidas da v1, Roadmap de v2 (OCR, extração de imagens com descrição via LLM, deploy online).
4. Linter `ruff check backend tests` passa sem erros; `ruff format --check` passa sem alterações pendentes.
5. Toda a suíte de testes (`pytest`) roda em menos de 60 segundos em máquina de desenvolvimento típica.
6. QA Gate rodado por `@qa` retorna verdict PASS ou CONCERNS (não FAIL).
7. O autor executa manualmente 5 fluxos reais de ponta a ponta (3 tipos de fonte + fallback de path + 1 caso de erro) e confirma que cada um produz o resultado esperado.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1 — Adicionar fixtures de corner cases (AC: 1)
  - [x] `tests/fixtures/scanned.pdf` — PDF 1 página só-imagem (~50KB), commitado, para caso (c)
  - [x] Confirmar que `sample.html` e `sample.pdf` (criados em Story 1.2) continuam válidos
- [x] Task 2 — Ampliar `tests/test_endpoints.py` com corner cases (AC: 1, 2)
  - [x] `test_extract_url_returns_404_maps_to_502` — mock HTTP → 404 → `SOURCE_FETCH_FAILED` (caso a)
  - [x] `test_extract_empty_html_maps_to_422` — mock HTTP retorna HTML quase-vazio → `CONVERSION_FAILED` com mensagem sobre SPA não suportada (caso b)
  - [x] `test_extract_scanned_pdf_maps_to_422_with_ocr_hint` — upload `scanned.pdf` → `CONVERSION_FAILED` com mensagem clara "OCR not enabled in v1" (caso c, AC2)
  - [x] `test_extract_non_pdf_file_maps_to_400` — upload `.txt` → `INVALID_INPUT` (caso d)
  - [x] `test_extract_oversized_upload_maps_to_413` — já existe em Story 1.4; confirmar cobertura (caso e)
  - [x] `test_extract_conversion_timeout_maps_to_504` — mock `_run_docling` dorme além do timeout → `REQUEST_TIMEOUT` (caso f)
- [x] Task 3 — Ajustar mensagens de erro para atender AC2 (caso c)
  - [x] Revisar `ERROR_CATALOG["CONVERSION_FAILED"]` — `hint` já contém "scanned PDF (OCR not supported in v1)" (arch §10.2)
  - [x] Garantir que ao detectar markdown vazio pós-Docling, o service levante `ConversionError` com mensagem "PDF appears to be scanned without extractable text. OCR is not enabled in v1." (arch §10.4)
  - [x] Ajustar `docling_service.extract` se necessário: pós-extract, se `markdown.strip()` estiver vazio ou abaixo de threshold (ex: <10 chars significativos), levantar `ConversionError`
- [x] Task 4 — Escrever/Atualizar `README.md` completo (AC: 3)
  - [x] Seção **Visão geral** — o que é, para quê, relação com o framework de conhecimento
  - [x] Seção **Pré-requisitos** — Python 3.11+, ~1GB de disco livre para modelos Docling, conexão de rede no primeiro boot
  - [x] Seção **Instalação** — `uv venv` / `uv pip install -e ".[dev]"` OU `pip install -e ".[dev]"`
  - [x] Seção **Como rodar** — `uvicorn backend.main:app --host 127.0.0.1 --port 8000`; nota sobre `--host 0.0.0.0` apenas se intencional (R8)
  - [x] Seção **Como usar** — passo a passo com 3 modos (URL, drag-drop, local_path) + menção explícita ao Swagger UI em `http://127.0.0.1:8000/docs` (S4 / AC3)
  - [x] Seção **Troubleshooting**:
    - [x] Primeiro boot baixa ~600MB de modelos (R2 / NFR6) — normal, pode demorar 2–5 min
    - [x] PDFs scanned não funcionam — sem OCR na v1
    - [x] URLs SPA/JS-rendered não suportadas — Docling não executa JS
    - [x] Erro "port already in use" — outro uvicorn rodando
  - [x] Seção **Estrutura do projeto** — árvore simplificada (refletindo arch §9)
  - [x] Seção **Limitações conhecidas v1** — OCR, batch, imagens, auth, mobile, escrita no vault
  - [x] Seção **Roadmap v2** — OCR, image extraction com LLM, deploy online (HF Spaces / Fly.io)
  - [ ] Screenshots opcionais (não bloqueiam AC) — deferred; não necessários para AC3
- [x] Task 5 — Garantir que `ruff` passa (AC: 4)
  - [x] Rodar `ruff check backend tests` — corrigir todos os issues reportados
  - [x] Rodar `ruff format backend tests` — aplicar formatação
  - [x] Rodar `ruff format --check backend tests` — deve passar sem alterações pendentes
  - [x] Opcional: adicionar seção `[tool.ruff]` em `pyproject.toml` com config mínima (line-length, select/ignore) — já presente desde 1.1
- [x] Task 6 — Validar tempo de execução da suíte (AC: 5)
  - [x] `time pytest` — verificar <60s (atual: 10.32s)
  - [x] Se ultrapassar: identificar testes lentos (`pytest --durations=10`), mockar Docling real, marcar testes lentos com `@pytest.mark.slow` e excluir do default com `pytest -m "not slow"` em `pyproject.toml` `[tool.pytest.ini_options]` — `markers = ["slow: ..."]` + `addopts = "-m 'not slow'"` adicionados preventivamente
- [ ] Task 7 — Rodar QA Gate via @qa (AC: 6)
  - [ ] Handoff para `@qa *qa-gate` sobre a story 1.6 (e revisão geral do epic)
  - [ ] Aceitar verdict PASS ou CONCERNS; se FAIL, voltar para @dev com feedback
- [ ] Task 8 — Validação manual E2E (AC: 7) — 5 fluxos reais
  - [ ] Fluxo 1: URL HTML real (ex: artigo do Wikipedia ou blog) → verificar `.md` gerado, frontmatter correto, corpo faz sentido
  - [ ] Fluxo 2: URL PDF remoto real (ex: paper acadêmico público) → idem
  - [ ] Fluxo 3: Upload drag-and-drop PDF local → idem
  - [ ] Fluxo 4: `local_path` fallback com PDF absoluto → idem
  - [ ] Fluxo 5: Caso de erro real (URL 404 ou PDF scanned) → verificar result-card--error com code/message/hint corretos
  - [x] Checklist documentado em `docs/qa/story-1.6-e2e-validation.md` (preparado por @dev; execução dos 5 fluxos é responsabilidade do autor)

## Dev Notes

### Previous Story Insights

Stories 1.1–1.5 já entregaram a funcionalidade completa. Story 1.6 é **hardening** (corner cases explícitos + docs + QA gate). Não há features novas; apenas:
- Mais testes de integração para casos de falha conhecidos.
- Ajuste fino de mensagens de erro (AC2 — caso scanned PDF).
- Documentação user-facing completa (README).
- Lint limpo.
- Validação manual E2E.

### Corner Case Mapping

[Source: arch §10.4 + PRD §6 Story 1.6 AC1]

| Caso | Exception | HTTP | Mensagem exata sugerida |
|------|-----------|------|-------------------------|
| (a) URL 404 | `SourceFetchError` | 502 | "URL returned 404. Verify the URL is publicly accessible." |
| (b) HTML vazio / SPA | `ConversionError` | 422 | "Extracted content is empty. JavaScript-rendered pages (SPA) are not supported in v1." |
| (c) PDF scanned | `ConversionError` | 422 | "PDF appears to be scanned without extractable text. OCR is not enabled in v1." |
| (d) Non-PDF upload | `InvalidInputError` | 400 | "Uploaded file is not a valid PDF." |
| (e) Upload > MAX | `UploadTooLargeError` | 413 | "Upload exceeds MAX_UPLOAD_MB (50 MB)." |
| (f) Timeout | `RequestTimeoutError` | 504 | "Extraction exceeded REQUEST_TIMEOUT_SECONDS (60s)." |

Todos respeitam o envelope `{status, code, message, hint}` com `hint` sempre presente (arch §10.2).

### Detecção de "markdown vazio" (AC2)

[Source: arch §10.4 — caso (c)]

O caso crítico é PDF scanned: Docling recebe o PDF, processa as páginas como imagens (sem OCR) e retorna markdown **vazio ou quase-vazio**. Sem detecção explícita, o file_writer escreveria um `.md` com só o frontmatter e corpo vazio.

**Solução:** em `backend/docling_service.py::extract`, após obter `markdown, metadata` do `_run_docling`, verificar:

```python
content_len = len(markdown.strip())
if content_len < 10:  # threshold arbitrário; tune conforme sample real
    raise ConversionError(
        "PDF appears to be scanned without extractable text. OCR is not enabled in v1."
    )
```

Testar via fixture `tests/fixtures/scanned.pdf` (criar nesta story) — verificar que o request POST retorna 422 + mensagem esperada.

### README Structure

[Source: PRD §6 Story 1.6 AC3 + arch §13]

Ordem recomendada:

```
# Docling Extractor

## Visão geral
Wrapper local sobre Docling para extração de .md com frontmatter mínimo.
Pipeline: URL ou PDF → Docling → .md no ./output/. Stateless, single-user.

## Pré-requisitos
- Python 3.11+
- ~1GB de disco livre (modelos Docling)
- Conexão de rede no primeiro boot (download de modelos)

## Instalação
```bash
uv venv
uv pip install -e ".[dev]"
# ou: python -m venv .venv && pip install -e ".[dev]"
```

## Como rodar
```bash
uv run uvicorn backend.main:app --host 127.0.0.1 --port 8000
```
Abrir http://127.0.0.1:8000.

⚠️ Para expor na LAN, mudar host para `0.0.0.0`. Default 127.0.0.1 por segurança (R8).

## Como usar
3 modos na UI:
1. Colar URL → Extract
2. Drag-and-drop PDF → conversão automática
3. Colar caminho absoluto no fallback → Extract

Swagger UI disponível em `http://127.0.0.1:8000/docs` para inspeção direta do endpoint `/extract` (S4).

## Troubleshooting
- **Primeiro boot lento (~2-5 min):** Docling está baixando modelos (~600MB). Normal (NFR6).
- **PDF não gera conteúdo:** pode ser scanned (sem texto extraível). OCR não está habilitado na v1.
- **URL não gera conteúdo:** página pode ser SPA com render em JS. Docling não executa JS.
- **"Address already in use":** outro uvicorn rodando; matar processo ou usar `--port 8001`.

## Estrutura do projeto
(árvore simplificada — refletindo arch §9)

## Limitações conhecidas v1
- Sem OCR (PDFs escaneados)
- Sem batch (1 fonte por requisição)
- Imagens ignoradas
- Sem autenticação / multi-user
- Desktop only
- Não escreve direto no vault (intencional — usuário move manualmente)

## Roadmap v2
- OCR para PDFs escaneados
- Extração de imagens com descrição via LLM
- Deploy online (HuggingFace Spaces / Fly.io)
```

### Ruff Configuration (recomendado — não AC bloqueante)

[Source: PRD §4 coding quality + arch §4]

Adicionar em `pyproject.toml`:

```toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]  # pycodestyle, pyflakes, isort, pyupgrade, flake8-bugbear
ignore = []
```

### Test Performance

[Source: arch §12.3 + PRD §6 Story 1.6 AC5]

- Mockar `_run_docling` universalmente na suíte default
- Testes `slow` (invocam Docling real): marcar com `@pytest.mark.slow`
- Em `pyproject.toml`:
  ```toml
  [tool.pytest.ini_options]
  addopts = "-m 'not slow'"
  markers = ["slow: tests that invoke real Docling (excluded by default)"]
  ```
- Rodar `pytest --durations=10` para identificar gargalos se ultrapassar 60s

### QA Gate

[Source: `.claude/rules/story-lifecycle.md` Phase 4]

@qa valida 7 quality checks:
1. Code review (patterns, readability)
2. Unit tests (coverage, passing)
3. Acceptance criteria (52 ACs do Epic 1)
4. No regressions
5. Performance (NFR3 <30s típico, AC5 suite <60s)
6. Security (NFR7 — single-user local, bind 127.0.0.1)
7. Documentation (README completo)

Verdict aceitável: PASS ou CONCERNS (nunca FAIL). Se FAIL, retornar para @dev com feedback específico.

### Manual E2E Validation

[Source: PRD §6 Story 1.6 AC7 + arch §12.4]

5 fluxos reais (não mockados):
1. URL HTML
2. URL PDF remoto
3. Upload PDF via drag-and-drop
4. local_path fallback
5. Caso de erro (URL 404 OU PDF scanned)

Para cada fluxo, verificar:
- Resposta aparece no `#result` sem reload
- Arquivo criado em `./output/` com nome slug correto
- Frontmatter YAML válido com campos obrigatórios + condicionais conforme detectado
- Corpo markdown faz sentido e não está vazio (exceto caso scanned, que retorna erro)

### File Locations

[Source: arch §9]

| Arquivo | Status nesta story |
|---------|---------------------|
| `README.md` | Atualizar (substancialmente) |
| `pyproject.toml` | Atualizar (adicionar `[tool.ruff]`, `[tool.pytest.ini_options]` se faltarem) |
| `backend/docling_service.py` | Atualizar (empty-markdown guard para AC2) |
| `tests/fixtures/scanned.pdf` | Novo |
| `tests/test_endpoints.py` | Atualizar (6 corner case tests) |
| `docs/qa/story-1.6-e2e-validation.md` | Novo (opcional — checklist manual) |

### Testing

[Source: arch §12]

- **Test file:** `tests/test_endpoints.py` (ampliado)
- **Framework:** `pytest` + `TestClient`
- **Fixtures novas:** `scanned.pdf` (~50KB, commitado)
- **Mocks:** `_run_docling` retorna string vazia para simular caso (c); `httpx.MockTransport` para casos (a) e (b)
- **Marker `slow`:** 1–2 testes smoke que realmente invocam Docling podem existir mas ficam excluídos do default
- **Performance target:** suíte completa <60s (AC5)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-18 | 1.0 | Draft inicial criado a partir do Epic 1 / PRD v1.3. | River (@sm) |
| 2026-04-19 | 1.1 | PO validation: GO (10/10). Status Draft → Ready. | Pax (@po) |
| 2026-04-19 | 1.2 | Implementação Tasks 1–6 concluída. Status Ready → InProgress → Ready for Review. Tasks 7–8 (QA gate + manual E2E) aguardam @qa e autor. | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (Opus 4.7, 1M context)

### Debug Log References

- Baseline antes da mudança: 79 testes PASS em 14.55s. Ruff `check` limpo;
  `format --check` reportou 7 arquivos por reformatar.
- Após adicionar o guard `_guard_non_empty_markdown`: um teste existente
  (`test_http_client_sends_user_agent` em `test_docling_service.py`)
  falhou porque usava `markdown="# x\n"` (3 chars após strip < 10). Foi
  ajustado para `"# Title\n\nBody paragraph extracted by the mock.\n"`
  — o objetivo desse teste é validar headers HTTP, não conteúdo.
- Fixture `scanned.pdf` gerada por script ad-hoc (`/tmp/make_scanned_pdf.py`,
  não commitado): PDF 1.4 válido, 1 página, content stream `q Q\n` (zero
  operadores de texto). 441 bytes. Abre corretamente via pypdfium2 como
  `len(doc) == 1`. O tamanho alvo sugerido (~50KB) é aspiracional; o que
  importa para o teste é ser válido estruturalmente — o mock de
  `_run_docling` simula a saída vazia.
- `pyproject.toml` ganhou `markers = ["slow: ..."]` + `addopts =
  "-m 'not slow'"` preventivamente. Nenhum teste marcado como slow na
  v1 (toda a suíte usa mocks e roda em ~10s).

### Completion Notes List

- **AC1**: todos os 6 corner cases (a)–(f) cobertos em `tests/test_endpoints.py`
  (3 novos testes adicionados; 3 existentes renomeados para alinhamento com
  o nomenclature do Task 2). Contagem final: 82 testes PASS.
- **AC2**: guard `_guard_non_empty_markdown` em `backend/docling_service.py`
  dispara `ConversionError` com mensagem específica ("OCR is not enabled in
  v1" para PDFs, "SPA...JavaScript-rendered" para HTML) quando o markdown
  pós-Docling tem <10 caracteres significativos. O threshold e o critério de
  separação por `source kind` foram documentados no comentário adjacente e no
  módulo.
- **AC3**: `README.md` reescrito substancialmente. 10 seções conforme story
  (Visão geral, Pré-requisitos, Instalação, Como rodar, Como usar com
  menção ao Swagger UI em `/docs`, Troubleshooting com 5 entradas, Estrutura
  do projeto refletindo arch §9, Limitações v1, Roadmap v2, Testes). Versão
  anterior (scaffolding Story 1.1) tinha 84 linhas; nova tem 185 linhas.
- **AC4**: `ruff check` passa (0 issues); `ruff format` reformatou 7 arquivos
  (6 backend/tests + 1 novo) e `ruff format --check` passa sem alterações
  pendentes.
- **AC5**: suíte completa em 10.32s (alvo: <60s). Folga de ~83%.
- **AC6**: aguarda handoff para @qa. Todos os pré-requisitos atendidos
  (todos os testes unit/integration PASS, lint limpo, cobertura de corner
  cases explícita, README completo).
- **AC7**: não executável por mim — requer máquina/rede/PDFs do autor.
  Checklist formal preparado em `docs/qa/story-1.6-e2e-validation.md` com
  critérios de verificação por fluxo.

### File List

**Modificados:**
- `backend/docling_service.py` — adicionado `_guard_non_empty_markdown` + constante `_MIN_MARKDOWN_CHARS`; chamada do guard inserida após todas as branches de extract; formatação aplicada por ruff.
- `tests/test_endpoints.py` — 3 novos testes de corner case (empty HTML, scanned PDF, non-PDF upload); 3 testes existentes renomeados (`unreachable_url_returns_502` → `url_returns_404_maps_to_502`, `oversized_upload_returns_413` → `oversized_upload_maps_to_413`, `timeout_returns_504` → `conversion_timeout_maps_to_504`); formatação aplicada por ruff.
- `tests/test_docling_service.py` — ajuste do `markdown` em `test_http_client_sends_user_agent` para evitar o novo guard de markdown vazio (objetivo do teste é headers HTTP, não conteúdo); formatação aplicada por ruff.
- `pyproject.toml` — adicionados `addopts = "-m 'not slow'"` e `markers = [...]` em `[tool.pytest.ini_options]`.
- `README.md` — reescrito do zero com as 10 seções exigidas pelo AC3.
- `backend/main.py`, `tests/test_errors.py`, `tests/test_file_writer.py`, `tests/test_frontmatter.py` — apenas formatação aplicada por `ruff format`.

**Criados:**
- `tests/fixtures/scanned.pdf` — PDF 1.4 válido, 1 página, 441 bytes, sem operadores de texto (simula scanned PDF sem OCR).
- `docs/qa/story-1.6-e2e-validation.md` — checklist E2E para execução manual do AC7 (5 fluxos).

## QA Results

### 2026-04-19 — Gate Verdict: **PASS** (Quinn, @qa)

**Gate file:** `docs/qa/gates/1.6-qa-hardening-documentation.yml`

**Summary:** Technical ACs 1–5 fully met; AC6 is this gate (PASS); AC7
is by design the author's manual sign-off (deferred, not blocking).

**Evidence (re-run independently in this gate):**
- `pytest -q` → **82 passed in 8.89 s** (AC5: <60 s target, 85% headroom).
- `ruff check backend tests` → **All checks passed!** (AC4).
- `ruff format --check backend tests` → **15 files already formatted** (AC4).
- All six Task-2 corner-case tests present with the exact names
  prescribed in the story (AC1 cases a–f):
  - `test_extract_url_returns_404_maps_to_502` (a, L177)
  - `test_extract_empty_html_maps_to_422` (b, L343)
  - `test_extract_scanned_pdf_maps_to_422_with_ocr_hint` (c, L365)
  - `test_extract_non_pdf_file_maps_to_400` (d, L384)
  - `test_extract_oversized_upload_maps_to_413` (e, L168)
  - `test_extract_conversion_timeout_maps_to_504` (f, L218)
- AC2 OCR-hint message enforced by `_guard_non_empty_markdown` in
  `backend/docling_service.py`, with source-kind-specific messages
  (SPA for `url_html`, "OCR not enabled in v1" for all PDF kinds).
- AC3 README has all 10 required sections plus the explicit Swagger
  UI subsection pointing at `http://127.0.0.1:8000/docs` (S4).
- `tests/fixtures/scanned.pdf`: valid PDF 1.4, 1 page, 441 B,
  `%PDF-` magic bytes confirmed, opens cleanly in pypdfium2.

**7 quality checks:** all PASS (code review, unit tests, AC coverage,
no regressions, performance, security, documentation). See gate file
for per-check notes.

**Top issues (all severity LOW — advisory only, non-blocking):**

| ID | File | Issue |
|----|------|-------|
| MNT-001 | `backend/docling_service.py` | `_MIN_MARKDOWN_CHARS = 10` threshold is arbitrary; theoretical false-positive for one-word legitimate docs. Accepted design per Dev Notes; trade-off documented in the adjacent comment. |
| MNT-002 | `backend/docling_service.py` | After `ruff format`, `HTTP_ACCEPT` is laid out as two adjacent string literals on one line. Semantically identical at runtime; cosmetic only. |
| MNT-003 | `tests/test_docling_service.py` | `test_http_client_sends_user_agent` stub markdown expanded from 3 → 42 chars so the new guard does not trigger; the test's header-propagation contract is unchanged. Informational only. |
| DOC-001 | `backend/errors.py` | `ERROR_CATALOG["CONVERSION_FAILED"]` hint is deliberately generic ("corrupted, empty, or scanned") and complements the per-exception specific `message`. No change needed; flagged for future polish. |

**Before merge/push:**
- [x] Author: execute `docs/qa/story-1.6-e2e-validation.md` and record
      the 5-flow verdict in that file (AC7 sign-off).
      → **Confirmed PASS 2026-04-19** by Vinicius (author). Interesting
      side-observation: a 7.3 MB book PDF (Christopher Alexander, ~100
      pages) hung the conversion beyond 300 s REQUEST_TIMEOUT — legitimate
      CPU-bound Docling limitation, documented in README §Limitações v1
      and a good driver for backlog E-001 (GPU acceleration). Smaller
      PDFs worked on all five flows.

**Status update:** AC7 now satisfied → 7/7 ACs met → ready for
`@devops *push` to close Epic 1.

— Quinn, guardião da qualidade 🛡️
