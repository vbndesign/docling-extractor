# Story 1.2: Core Docling Extraction Service

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [pytest, ruff, coderabbit, docling-fixtures]
```

## Story

**As** the backend,
**I want** a unified service function that accepts any of the 3 input types (HTML URL, PDF URL, local PDF file) and returns a tuple of (markdown_body, raw_metadata),
**so that** the upper layers of the app (endpoints, writer) have a single clean interface to Docling regardless of source type.

## Acceptance Criteria

1. `backend/docling_service.py` expõe uma função pública `extract(source)` que aceita: (a) string URL para página HTML, (b) string URL para PDF remoto, (c) caminho local ou file-like object para PDF local.
2. A função detecta o tipo de input (HEAD request para URLs + checagem de Content-Type; detecção de extensão/MIME para arquivos locais) e roteia para a pipeline apropriada do Docling.
3. Retorna um dataclass ou dict com duas chaves: `markdown` (string com o conteúdo Markdown puro gerado pelo Docling) e `metadata` (dict com os campos detectados — ex: `source_title`, `author`, `year` — omitindo os não detectados).
4. Usa a API `DocumentConverter` do Docling conforme o exemplo `minimal.py` oficial, sem customizações de pipeline. OCR desabilitado.
5. Imagens dentro das fontes são ignoradas — o Markdown gerado contém apenas texto e estrutura.
6. Testes unitários em `tests/test_docling_service.py` usam fixtures (`tests/fixtures/sample.html` e `tests/fixtures/sample.pdf`) pequenas e commitadas no repo, exercitando os 3 caminhos.
7. Testes cobrem o caso de sucesso para cada tipo e pelo menos um caso de falha explícita (ex: arquivo corrompido retorna exception bem definida).
8. A função lança exceptions tipadas (ex: `SourceFetchError`, `ConversionError`) em vez de deixar vazar exceptions genéricas do Docling — isso prepara o tratamento de erro nas camadas superiores.
9. Cliente HTTP externo (`httpx`) usado para fetch de URLs remotas está configurado com (S3): (a) headers explícitos — `User-Agent` com string Mozilla/Chrome-compatível (bare-token UAs são rejeitados com 403 por publishers protegidos por Cloudflare, ex.: nngroup.com), `Accept` cobrindo pelo menos `text/html` e `application/pdf`, `Accept-Language: en-US,en`; (b) timeout total de 30 segundos com `connect_timeout` de 10 segundos; (c) seguir redirects HTTP com limite de 5 hops. Essas configurações são aplicadas via `httpx.AsyncClient` instanciado no nível da aplicação.
   - **Nota histórica (revisão 2026-04-19):** v1.0–v1.3 desta story exigiam `User-Agent: docling-extractor/1.0`. Descoberto em uso real que esse valor gerava 403 em sites alvo (nngroup.com). AC revisto para aceitar UA browser-compatível; intenção S3 (UA explícito, timeout bounded, redirect cap) preservada. A identificação "docling-extractor" foi trocada por compatibilidade operacional — se telemetria opt-in for necessária no futuro, endereçar via header custom (`X-Extractor`) em story nova.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1 — Criar `backend/models.py` com dataclasses (AC: 3)
  - [x] `ExtractedMetadata(source_title, author, year)` — todos `Optional`, `frozen=True`
  - [x] `SourceDescriptor(kind, location, original_filename)` — `kind: Literal["url_html","url_pdf","pdf_upload","pdf_local_path"]`
  - [x] `ExtractionResult(markdown, metadata, source)` — output de `extract`
- [x] Task 2 — Criar `backend/errors.py` com a hierarquia de exceptions (AC: 8)
  - [x] Base `DoclingExtractorError` com atributos `code: str`, `http_status: int`
  - [x] Subclasses: `InvalidInputError`, `SourceFetchError`, `ConversionError` (demais virão em Story 1.4)
  - [x] NÃO implementar `register_handlers` aqui — apenas a hierarquia; o handler FastAPI virá em Story 1.4
- [x] Task 3 — Implementar `backend/docling_service.py::extract` (AC: 1, 2, 4, 5)
  - [x] Assinatura: `async def extract(source, *, http_client, converter) -> ExtractionResult` (arch §6.3)
  - [x] Detecção de tipo:
    - [x] Se `source` é `str` começando com `http://` ou `https://`: HEAD request → inspecionar `Content-Type`
    - [x] Fallback (R3): se HEAD falhar (405, erro de rede), GET com `Range: bytes=0-1023` + magic bytes (`%PDF-` para PDF, `<` para HTML)
    - [x] Se `source` é `Path` ou `str` que é path válido: verificar extensão `.pdf` + magic bytes
    - [x] Se `source` é file-like (tem `.read()`): tratar como PDF upload
  - [x] Wrap chamada Docling em função privada `_run_docling(source_bytes_or_path) -> (markdown, metadata)` (arch §2.1 A3) para permitir mock em testes
  - [x] Usar `DocumentConverter()` com defaults (sem OCR, sem custom pipeline)
  - [x] Traduzir exceptions: falha de fetch → `SourceFetchError`; falha de conversão → `ConversionError`
- [x] Task 4 — Configurar `httpx.AsyncClient` no nível da aplicação (AC: 9)
  - [x] Factory `make_http_client() -> httpx.AsyncClient` com:
    - [x] `headers={"User-Agent": "docling-extractor/1.0"}`
    - [x] `timeout=httpx.Timeout(30.0, connect=10.0)`
    - [x] `follow_redirects=True`, `max_redirects=5`
  - [x] Documentar que esse client será instanciado no `lifespan` do FastAPI em Story 1.4
- [x] Task 5 — Adicionar fixtures de teste (AC: 6)
  - [x] `tests/fixtures/sample.html` (<5KB) — HTML mínimo com `<title>`, `<meta name="author">`, corpo com `<h1>`, parágrafos
  - [x] `tests/fixtures/sample.pdf` (~20KB) — PDF curto com texto real (não scanned)
  - [x] Ambos commitados no repositório
- [x] Task 6 — Criar `tests/conftest.py` inicial (AC: 6, 7)
  - [x] Fixture `mock_docling_result` retornando `(markdown, metadata)` fixo
  - [x] Fixture `mock_http_transport` usando `httpx.MockTransport` para interceptar URLs
  - [x] Fixture `test_http_client` usando o transport acima
- [x] Task 7 — Escrever `tests/test_docling_service.py` (AC: 6, 7)
  - [x] `test_extract_html_url_success` — URL HTML mockada retorna `ExtractionResult` com markdown + metadata
  - [x] `test_extract_pdf_url_success` — URL PDF mockada
  - [x] `test_extract_local_pdf_success` — path para `sample.pdf`
  - [x] `test_extract_head_fallback_to_magic_bytes` (R3) — HEAD retorna 405, GET Range retorna magic bytes
  - [x] `test_extract_corrupted_pdf_raises_conversion_error` (AC7 falha)
  - [x] `test_extract_unreachable_url_raises_source_fetch_error` (AC7 falha)
  - [x] `test_http_client_uses_user_agent_and_timeout` (AC9) — inspecionar request feita via `MockTransport`

## Dev Notes

### Previous Story Insights

Story 1.1 criou estrutura base. `backend/config.py::Settings` e `get_settings()` já existem. Esta story adiciona `backend/models.py`, `backend/errors.py`, `backend/docling_service.py` e o primeiro `tests/conftest.py`.

### Domain Dataclasses

[Source: arch §5.1]

```python
# backend/models.py
from dataclasses import dataclass
from pathlib import Path
from typing import Literal, Optional

@dataclass(frozen=True)
class ExtractedMetadata:
    """Metadados detectados automaticamente. None ⇒ frontmatter omite o campo."""
    source_title: Optional[str] = None
    author: Optional[str] = None
    year: Optional[int] = None

@dataclass(frozen=True)
class SourceDescriptor:
    """Descreve a origem — usado por file_writer para derivar nome e por frontmatter para `location`."""
    kind: Literal["url_html", "url_pdf", "pdf_upload", "pdf_local_path"]
    location: str                           # URL, nome do arquivo, ou path absoluto
    original_filename: Optional[str] = None # apenas para pdf_upload e pdf_local_path

@dataclass(frozen=True)
class ExtractionResult:
    """Output de docling_service.extract."""
    markdown: str
    metadata: ExtractedMetadata
    source: SourceDescriptor
```

### Exception Hierarchy (parcial nesta story)

[Source: arch §10.1]

```python
# backend/errors.py
class DoclingExtractorError(Exception):
    code: str = "INTERNAL_ERROR"
    http_status: int = 500

class InvalidInputError(DoclingExtractorError):
    code = "INVALID_INPUT"; http_status = 400

class SourceFetchError(DoclingExtractorError):
    code = "SOURCE_FETCH_FAILED"; http_status = 502

class ConversionError(DoclingExtractorError):
    code = "CONVERSION_FAILED"; http_status = 422
```

`UploadTooLargeError`, `RequestTimeoutError` e `ERROR_CATALOG` ficam para Story 1.4.

### Interface Contract

[Source: arch §6.3]

```python
async def extract(
    source: str | Path | BinaryIO,
    *,
    http_client: httpx.AsyncClient,
    converter: DocumentConverter,
) -> ExtractionResult: ...
```

**Invariantes:**
- Service **não** escreve em disco (R1/§2.1 (c)).
- Service **não** levanta `HTTPException` (arch §10.3).
- Converter e http_client são injetados (lifespan-scoped singletons vindos do `main.py` em Story 1.4).

### Type Detection Flow

[Source: arch §2.2 R3 + PRD §6 Story 1.2 AC2]

1. URL string → HEAD request
2. HEAD bem-sucedido:
   - `Content-Type: text/html*` → pipeline HTML
   - `Content-Type: application/pdf` → pipeline PDF URL
3. HEAD falha (405, 403, erro de rede) → GET com `Range: bytes=0-1023`:
   - Magic bytes `%PDF-` → PDF
   - Primeiro caractere `<` → HTML
4. Path local:
   - Extensão `.pdf` + magic bytes `%PDF-` confirmam PDF
   - Qualquer outro → `InvalidInputError`
5. File-like (tem `.read()`) → tratar como PDF upload; ler bytes e validar magic

### Docling Usage

[Source: arch §4, PRD §6 Story 1.2 AC4]

- Usar `docling.document_converter.DocumentConverter` — API de alto nível.
- Sem customização de pipeline (sem OCR, sem figure extraction, sem custom parsers).
- Converter é **injetado**, não instanciado dentro do service (§3.4, lifespan singleton evita cold start).
- CPU-bound: em Story 1.4 o endpoint chamará esta função via `asyncio.to_thread`.

### Encapsulation for Testability

[Source: arch §2.1 A3]

```python
def _run_docling(source_for_docling, *, converter: DocumentConverter) -> tuple[str, dict]:
    """Private wrapper. Retorna (markdown, metadata_dict). Mockable nos testes."""
    ...
```

Testes mockam `_run_docling` via `monkeypatch` ou fixture.

### HTTP Client Config

[Source: arch §2.2 R7 + PRD §6 Story 1.2 AC9]

```python
def make_http_client() -> httpx.AsyncClient:
    return httpx.AsyncClient(
        headers={"User-Agent": "docling-extractor/1.0"},
        timeout=httpx.Timeout(30.0, connect=10.0),
        follow_redirects=True,
        max_redirects=5,
    )
```

Factory fica em `backend/docling_service.py` (ou `backend/http.py` se o @dev preferir). Em Story 1.4, o `lifespan` do app cria e fecha essa instância.

### File Locations

[Source: arch §6.1 Component Inventory + §9 Source Tree]

| Arquivo | Cria nesta story? | Notas |
|---------|-------------------|-------|
| `backend/models.py` | Sim (novo) | Dataclasses |
| `backend/errors.py` | Sim (parcial) | Apenas base + 3 subclasses; resto em Story 1.4 |
| `backend/docling_service.py` | Sim (novo) | Função `extract` + `make_http_client` + `_run_docling` |
| `tests/conftest.py` | Sim (novo) | Fixtures compartilhadas |
| `tests/test_docling_service.py` | Sim (novo) | Unit tests |
| `tests/fixtures/sample.html` | Sim (novo) | <5KB |
| `tests/fixtures/sample.pdf` | Sim (novo) | ~20KB |

### Boundary Rules

[Source: arch §10.3]

1. `docling_service` só levanta `DoclingExtractorError` (ou subclasses). Nunca `HTTPException`.
2. Service nunca importa de `main.py`.
3. Logs ficam no handler global (Story 1.4), não aqui — evitar duplicação.

### Testing

[Source: arch §12.1 + §12.2]

- **Test file:** `tests/test_docling_service.py`
- **Framework:** `pytest` + `pytest-asyncio` (testes `async` usam `@pytest.mark.asyncio`)
- **Mocks:**
  - `_run_docling` mockado para evitar chamar Docling real (lento)
  - `httpx.MockTransport` para interceptar HTTP sem rede
- **Fixtures commitadas:** `tests/fixtures/sample.html`, `tests/fixtures/sample.pdf`
- **Marker `slow`:** 1–2 testes "fumaça" que realmente invocam Docling podem ser marcados `@pytest.mark.slow` e excluídos do default (`pytest -m "not slow"`)
- **Performance:** suíte inteira <60s (global, Story 1.6 AC5) — esta story isoladamente deve ficar <10s com mocks

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-18 | 1.0 | Draft inicial criado a partir do Epic 1 / PRD v1.3. | River (@sm) |
| 2026-04-18 | 1.1 | Validated GO (10/10, readiness 9.5/10). Status Draft → Ready. Should-Fix: AC5 needs explicit image-absence test assertion (advisory, non-blocking). | Pax (@po) |
| 2026-04-18 | 1.2 | Implementation complete. 12/12 tests pass (6.3s). Status Ready → Ready for Review. AC5 advisory addressed via `test_extract_html_result_contains_no_image_markup`. | Dex (@dev) |
| 2026-04-19 | 1.3 | QA Gate PASS (9/9 ACs, 12/12 tests, ruff clean). Status Ready for Review → Done. 3 low-severity advisory observations carried forward. | Quinn (@qa) |
| 2026-04-19 | 1.4 | Post-Done field change: `HTTP_USER_AGENT` switched from `docling-extractor/1.0` to a Chrome UA, plus `Accept` + `Accept-Language` headers. Cloudflare-fronted publishers (e.g. nngroup.com) return 403 for bare-token UAs. AC9 letter no longer matches (token removed); intent (identifiable + timeout + redirect cap) preserved. **Needs @po review to revise AC9 text.** | Dex (@dev) |
| 2026-04-19 | 1.5 | AC9 revised to match v1.4 implementation: bare-token `docling-extractor/1.0` replaced by Mozilla/Chrome UA + `Accept`/`Accept-Language`. S3 intent preserved. Historical note retained for traceability. Story remains Done; flagging @qa for a re-validation touch on gate 1.2. | Pax (@po) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (Opus 4.7, 1M context) — @dev persona Dex.

### Debug Log References

- `pytest tests/test_docling_service.py -v`: 12 passed in 6.31s (mocked `_run_docling`, `httpx.MockTransport` transport).
- `pytest -q` (full suite): 12 passed in 6.56s.
- `ruff check backend/ tests/`: clean (after auto-fix + manual PEP 604 conversions).
- Docling smoke call against `tests/fixtures/sample.pdf` round-tripped `"Hello Docling. This is a sample PDF used for unit tests."` (confirms the hand-crafted fixture is a valid PDF — first invocation triggers one-time model weight download; subsequent runs are local).

### Completion Notes List

- **AC1–AC3:** `backend/docling_service.py::extract` accepts URL strings (http/https), file-like objects (`.read()`), and local paths, returning `ExtractionResult(markdown, metadata, source)`. Metadata dataclass omits `None` fields via the frontmatter writer in Story 1.3.
- **AC2 + R3 fallback:** HEAD request with `Content-Type` inspection; on 4xx/network error, falls back to a ranged GET and sniffs `%PDF-` / `<` magic bytes. Covered by `test_extract_head_fallback_to_magic_bytes`.
- **AC4:** Uses `DocumentConverter` with defaults (no OCR, no custom pipeline). Converter is injected, never instantiated inside the service (`_run_docling(source, *, converter)`).
- **AC5 (advisory):** Added `test_extract_html_result_contains_no_image_markup` as the @po Should-Fix follow-up — asserts the returned markdown contains neither `![...]` nor `<img` when the source HTML embeds an image tag.
- **AC6–AC7:** 12 unit tests, 4 happy paths (url_html, url_pdf, pdf_upload, pdf_local_path) + 5 failure/edge paths (HEAD fallback, corrupted PDF, unreachable URL, unsupported local extension, upload without PDF magic) + 2 AC9 checks + 1 AC5 advisory.
- **AC8:** Service layer only raises `DoclingExtractorError` subclasses (`InvalidInputError`, `SourceFetchError`, `ConversionError`). Generic Docling/httpx exceptions are translated inside `_run_docling` / `_detect_url_kind` / `_fetch_bytes`. `UploadTooLargeError` and `RequestTimeoutError` deferred to Story 1.4 per scope.
- **AC9:** `make_http_client()` builds an `httpx.AsyncClient` with `User-Agent: docling-extractor/1.0`, `httpx.Timeout(30.0, connect=10.0)`, `follow_redirects=True`, `max_redirects=5`. Asserted by `test_make_http_client_uses_documented_config` (direct config inspection) and `test_http_client_sends_user_agent` (header observed via `MockTransport`).
- **Test fixture note:** `tests/fixtures/sample.pdf` is a hand-crafted 637-byte minimal PDF (valid header, xref, single Helvetica text object). The story suggested ~20KB but smaller is acceptable — the fixture is still a real, parseable PDF (confirmed by Docling smoke).
- **Lint/types:** ruff clean with the pyproject config (`E, F, W, I, UP, B`). No mypy gate configured in this story — type hints use PEP 604 (`str | None`) throughout.
- **Out of scope (deferred):** `HTTPException` mapping, global logging, upload-size guard, timeout exception type, `ERROR_CATALOG` — all handled in Story 1.4.

### File List

**Created:**

- `backend/models.py` — `ExtractedMetadata`, `SourceDescriptor`, `ExtractionResult`, `SourceKind`.
- `backend/errors.py` — `DoclingExtractorError`, `InvalidInputError`, `SourceFetchError`, `ConversionError`.
- `backend/docling_service.py` — `extract`, `make_http_client`, `_run_docling`, `_detect_url_kind`, `_fetch_bytes`, `_parse_html_meta`, `HTTP_USER_AGENT`, `PDF_MAGIC`.
- `tests/conftest.py` — `fixtures_dir`, `sample_html_path`, `sample_pdf_path`, `sample_html_bytes`, `sample_pdf_bytes`, `mock_docling_result`, `make_mock_transport`, `make_test_http_client`, `dummy_converter`.
- `tests/test_docling_service.py` — 12 unit tests.
- `tests/fixtures/sample.html` — 682 B HTML fixture with `<title>`, `<meta name="author">`.
- `tests/fixtures/sample.pdf` — 637 B minimal valid PDF fixture.

**Modified:** _(none — no pre-existing files changed)_

## QA Results

### Review Date: 2026-04-19

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is clean, focused, and honors every boundary rule defined in arch §2.2 and §10.3. Public surface is narrow — `extract`, `make_http_client`, and the two module-level constants (`HTTP_USER_AGENT`, `PDF_MAGIC`). Private helpers (`_run_docling`, `_detect_url_kind`, `_fetch_bytes`, `_parse_html_meta`, `_is_url`) cleanly separate concerns: type detection is decoupled from fetching, fetching is decoupled from conversion, and conversion is isolated behind the testability seam required by arch §2.1 A3. The service writes zero bytes to disk — URL bodies are streamed into Docling through `DocumentStream(name=..., stream=io.BytesIO(body))`, which satisfies R1. Exception translation is strict: every `httpx.RequestError` becomes `SourceFetchError`, every Docling exception becomes `ConversionError`, and generic leaks are impossible by construction.

### Refactoring Performed

None. The implementation does not warrant QA refactoring — the observations below are advisory, non-blocking, and scoped to future stories.

### Compliance Check

- Coding Standards: ✓ — PEP 604 union types, `from __future__ import annotations`, ruff clean on `backend/` and `tests/`.
- Project Structure: ✓ — files land exactly where arch §6.1 + §9 prescribe; no drift.
- Testing Strategy: ✓ — pytest + pytest-asyncio per arch §12; `_run_docling` monkey-patched; `httpx.MockTransport` isolates the network layer; committed fixtures exercise all 4 paths.
- All ACs Met: ✓ — 9/9 met, see gate file for per-AC evidence.

### Improvements Checklist

- [ ] (Story 1.3 candidate) If the frontmatter writer needs richer metadata than title+author (e.g., publication date, OpenGraph), migrate `_parse_html_meta` from regex to stdlib `html.parser`. Regex is adequate for current AC3 scope.
- [ ] (Story 1.6 candidate) Add `filterwarnings = ["ignore::DeprecationWarning:pytest_asyncio.*"]` to `pyproject.toml` — the current suite surfaces 630 deprecation lines from pytest-asyncio internals.
- [ ] (Cosmetic) Convert `_is_url` into a `TypeGuard[str]` so the `# type: ignore[assignment]` in `extract` can be dropped.

### Security Review

No concerns. All dependencies are injected (`http_client`, `converter`) — no module-level globals, no environment lookup at call time. The HTTP client caps blast radius with `max_redirects=5`, `timeout=30s total / 10s connect`, and a stable identifiable User-Agent. Local-path and upload code paths validate PDF magic bytes before handing data to Docling, which narrows the attack surface against malformed-file exploits. No shell, no subprocess, no eval.

### Performance Considerations

Warm-cache suite runs in 5.71s — consistent with the dev report of 6.31s and comfortably under the <10s target for this story. Cold run is ~76s, driven by Docling's module-level imports (model/weight initialization on first import), not by this story's code. This becomes relevant for Story 1.6 AC5 (global suite <60s) and should be handled there via either a warmed CI cache or a lazy Docling import inside `_run_docling`.

### Files Modified During Review

None. No QA refactors applied.

### Gate Status

Gate: **PASS** → `docs/qa/gates/1.2-core-extraction-service.yml`

Quality score: 100 (0 FAIL × 20 + 0 CONCERNS × 10).

Evidence:
- Tests reviewed: 12 (12 passed)
- Issues raised: 0
- Observations (low, advisory): 3
- AC coverage: 1, 2, 3, 4, 5, 6, 7, 8, 9 — zero gaps.

### Recommended Status

✓ **Ready for Done** — @dev should flip the Status field from "Ready for Review" to "Done" once @devops pushes the feature branch. No changes required.

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-19 | 1.0 | QA review: PASS (9/9 ACs, 12/12 tests, ruff clean). 3 low-severity advisory observations for future stories. | Quinn (@qa) |
| 2026-04-19 | 1.1 | Re-gate after AC9 revision (Change Log v1.4/v1.5): PASS. Trigger: bare-token `docling-extractor/1.0` UA replaced with Mozilla/Chrome UA + `Accept` + `Accept-Language` headers to bypass Cloudflare 403 on publisher sites (nngroup.com repro). Re-verification: AC9 tests 2/2 pass (9.23s), full suite 78/78 pass, ruff clean, live fetch of nngroup article returns 200 with 115,426-byte HTML. S3 security properties we rely on (bounded timeout, redirect cap, explicit UA) intact; publisher-side identification as "docling-extractor" consciously traded for operational reliability — future opt-in re-introduction via `X-Extractor` header recommended, non-blocking. Story remains `Done`. See gate file `re_gate_addendum` block for full evidence. | Quinn (@qa) |
