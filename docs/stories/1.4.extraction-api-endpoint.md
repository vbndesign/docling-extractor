# Story 1.4: Extraction API Endpoint

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [pytest, httpx, ruff]
```

## Story

**As a** frontend client,
**I want** a single HTTP endpoint that accepts either a URL or a multipart file upload and triggers the full extraction + write pipeline,
**so that** the UI can submit any of the 3 input types through one clean JSON contract.

## Acceptance Criteria

1. `backend/main.py` expõe `POST /extract` aceitando `multipart/form-data` com **três campos opcionais mutuamente exclusivos**: `url` (string), `file` (upload) ou `local_path` (string — caminho absoluto para PDF no sistema de arquivos local, atendendo ao fallback FR9).
2. O endpoint valida que exatamente um dos três campos foi fornecido — retorna HTTP 400 com mensagem clara se zero, dois ou três estiverem presentes. Para `local_path`, valida adicionalmente que o caminho existe, é legível e corresponde a um arquivo PDF (extensão `.pdf` ou MIME `application/pdf`); falhas retornam HTTP 400 com mensagem acionável.
3. Uploads excedendo `MAX_UPLOAD_MB` retornam HTTP 413 com mensagem clara. A verificação acontece antes de a conversão iniciar.
4. A rota integra, na ordem: `docling_service.extract` → `frontmatter.build` → `file_writer.save`, e retorna JSON `{"status": "ok", "output_path": "<absolute path>", "filename": "<basename>"}` com HTTP 200 em sucesso.
5. Exceptions tipadas da camada de serviço são mapeadas para respostas HTTP claras: `SourceFetchError` → 502, `ConversionError` → 422, qualquer outra → 500. Todas incluem mensagem textual acionável.
6. Timeout global por requisição respeitando `REQUEST_TIMEOUT_SECONDS` do config; excedê-lo retorna 504 com mensagem clara.
7. Testes de integração em `tests/test_endpoints.py` cobrem, via `TestClient` do FastAPI, os **4 cenários de sucesso** (URL HTML, URL PDF, upload PDF local, `local_path`) e os casos de erro (sem input, múltiplos inputs simultâneos, upload oversized, URL inacessível, `local_path` inexistente, `local_path` apontando para arquivo não-PDF).
8. Para cada caso de sucesso no teste, o arquivo é efetivamente criado no output dir (fixture aponta para diretório temporário) e o conteúdo é validado: frontmatter correto + corpo não-vazio.
9. O endpoint `POST /extract` aplica content negotiation via header `Accept`: se o cliente preferir `text/html` (HTMX default) e não preferir `application/json`, retorna partial Jinja2 (`frontend/templates/partials/result_success.html` em sucesso; `frontend/templates/partials/result_error.html` em erro, com status HTTP apropriado). Caso contrário, retorna o JSON definido em AC4/AC5. O handler global de exceptions respeita a mesma regra (referência normativa: arch doc §7.3 + §10.2). Testes cobrem: (a) `Accept: text/html` retorna HTML em sucesso e em erro; (b) ausência de `Accept` retorna JSON (preserva contrato AC4); (c) `Accept: application/json` retorna JSON.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1 — Completar `backend/errors.py` com catalog + handlers (AC: 2, 3, 5, 6, 9)
  - [x] Adicionar subclasses restantes: `UploadTooLargeError` (413), `RequestTimeoutError` (504)
  - [x] Definir `ERROR_CATALOG: dict[str, tuple[str, str]]` com 6 entradas (arch §10.2)
  - [x] Implementar `handler(request, exc) -> Response` respeitando `prefers_html(request)`
  - [x] Implementar `unexpected_handler(request, exc)` (catch-all → 500 INTERNAL_ERROR, com log)
  - [x] Implementar `register_handlers(app)` que registra ambos via `app.add_exception_handler`
  - [x] Criar partials Jinja placeholders (serão preenchidos visualmente em Story 1.5):
    - [x] `frontend/templates/partials/result_success.html` — mínimo: expor `{{ result.output_path }}`, `{{ result.filename }}`
    - [x] `frontend/templates/partials/result_error.html` — mínimo: expor `{{ code }}`, `{{ message }}`, `{{ hint }}`
- [x] Task 2 — Implementar `lifespan` e mounts em `backend/main.py` (AC: 4)
  - [x] `@asynccontextmanager lifespan(app)`:
    - [x] Criar `DocumentConverter()` e guardar em `app.state.converter` (R2)
    - [x] Criar `httpx.AsyncClient` via `make_http_client()` e guardar em `app.state.http_client`
    - [x] Criar `Jinja2Templates(directory="frontend/templates")` e guardar em `app.state.templates`
    - [x] Yield
    - [x] Ao shutdown: `await http_client.aclose()`
  - [x] Mount `app.mount("/static", StaticFiles(directory="frontend/static"), name="static")`
  - [x] Chamar `errors.register_handlers(app)` no setup
- [x] Task 3 — Implementar `POST /extract` (AC: 1, 2, 3, 4, 5, 6, 9)
  - [x] Assinatura com `Form(None)` + `File(None)` + `Depends(get_settings)`
  - [x] Validar exatamente 1 dos 3 campos preenchido → senão `InvalidInputError`
  - [x] Se `local_path`: verificar `Path(local_path).is_file()` + extensão `.pdf` + magic bytes `%PDF-` → senão `InvalidInputError`
  - [x] Se `file`: checar `file.size > MAX_UPLOAD_MB * 1024 * 1024` → `UploadTooLargeError` (antes de ler)
  - [x] Montar `source` conforme tipo detectado
  - [x] Timeout global via `asyncio.wait_for(_run_pipeline(...), timeout=settings.request_timeout_seconds)` (R1)
    - [x] `_run_pipeline` encadeia: `await extract(...)` → `build(...)` → `save(...)` (build+save em `asyncio.to_thread`)
    - [x] Timeout → `RequestTimeoutError`
    - [x] Docling bound: `_run_docling` agora chamado via `asyncio.to_thread` dentro de `docling_service.extract` (fecha ambiguidade S1 do PO v1.1)
  - [x] Resposta:
    - [x] Se `prefers_html(request)`: `templates.TemplateResponse("partials/result_success.html", {...})`
    - [x] Senão: `JSONResponse({"status": "ok", "output_path": str(path), "filename": name})`
  - [x] Nenhum `try/except` de tradução no corpo — handler global faz o mapping (o `except TimeoutError` apenas re-lança como domain error, sem produzir Response)
- [x] Task 4 — Adicionar `GET /` placeholder — **SKIPPED** (story autoriza pular; Story 1.5 finalizará o index.html)
- [x] Task 5 — Escrever `tests/test_endpoints.py` (AC: 7, 8, 9)
  - [x] Fixture `test_client` em `conftest.py` com `app.dependency_overrides[get_settings]` apontando para `tmp_path`
  - [x] Mockar `_run_docling` via `monkeypatch` para retornar markdown + metadata fixos
  - [x] Sucessos (4):
    - [x] `test_extract_url_html_success` — POST `url=...` (mock HTTP), verificar 200 + arquivo criado + frontmatter válido
    - [x] `test_extract_url_pdf_success`
    - [x] `test_extract_upload_pdf_success` — POST com `file=` (enviar bytes de fixture PDF)
    - [x] `test_extract_local_path_success` — POST `local_path=<path to fixture PDF>`
  - [x] Falhas:
    - [x] `test_extract_no_input_returns_400` — nenhum campo → 400 INVALID_INPUT
    - [x] `test_extract_multiple_inputs_returns_400` — url + file simultâneos
    - [x] `test_extract_oversized_upload_returns_413` — file.size > MAX → 413 UPLOAD_TOO_LARGE
    - [x] `test_extract_unreachable_url_returns_502` — mock HTTP retorna 404 → 502 SOURCE_FETCH_FAILED
    - [x] `test_extract_local_path_nonexistent_returns_400` — INVALID_INPUT
    - [x] `test_extract_local_path_not_pdf_returns_400` — arquivo `.txt` → 400 INVALID_INPUT
    - [x] `test_extract_timeout_returns_504` — mock `_run_docling` dorme além do timeout → 504
  - [x] Content negotiation (AC9):
    - [x] `test_extract_returns_json_when_no_accept_header`
    - [x] `test_extract_returns_json_when_accept_application_json`
    - [x] `test_extract_returns_html_when_accept_text_html_on_success`
    - [x] `test_extract_returns_html_when_accept_text_html_on_error`
  - [x] `test_health_endpoint_returns_ok` (sanity check — herdado de Story 1.1 mas re-testado após mounts)
- [x] Task 6 (adicionada pela revisão PO v1.1) — Escrever `tests/test_errors.py` (invariantes §10.2)
  - [x] `code` de cada subclasse ∈ `ERROR_CATALOG` (6 entradas)
  - [x] `hint` nunca null/vazio
  - [x] Envelope JSON sempre `{status, code, message, hint}` + mensagem default quando exception não carrega args
  - [x] `unexpected_handler` promove qualquer Exception → `INTERNAL_ERROR` (500)
  - [x] HTML e JSON partilham o mesmo payload (mesmos `code`, `message`, `hint` renderizados)
  - [x] `prefers_html` respeita tabela de precedência de `Accept`

## Dev Notes

### Previous Story Insights

Story 1.2 entregou `docling_service.extract` + exceptions base + http client factory. Story 1.3 entregou `frontmatter.build` + `file_writer.save` + `SaveResult`. Esta story liga tudo via `main.py` + completa `errors.py` com catalog/handlers e adiciona content negotiation.

### `POST /extract` Response Contract

[Source: arch §7.2, PRD §6 Story 1.4 AC4]

**200 JSON:**
```json
{
  "status": "ok",
  "output_path": "C:\\...\\output\\example.md",
  "filename": "example.md"
}
```

**Erros (body comum):**
```json
{ "status": "error", "code": "SOURCE_FETCH_FAILED", "message": "...", "hint": "..." }
```

| HTTP | code | Gatilho |
|------|------|---------|
| 400 | `INVALID_INPUT` | Zero ou múltiplos campos; `local_path` inválido |
| 413 | `UPLOAD_TOO_LARGE` | Upload > `MAX_UPLOAD_MB` |
| 422 | `CONVERSION_FAILED` | `ConversionError` |
| 502 | `SOURCE_FETCH_FAILED` | `SourceFetchError` |
| 504 | `REQUEST_TIMEOUT` | Excede timeout |
| 500 | `INTERNAL_ERROR` | Qualquer não mapeada |

### Content Negotiation (AC9)

[Source: arch §7.3]

```python
def _prefers_html(request: Request) -> bool:
    accept = request.headers.get("accept", "")
    return "text/html" in accept and "application/json" not in accept
```

**Regra de precedência:**

| Accept recebido | Response |
|---|---|
| contém `text/html` **e não** `application/json` | HTML partial |
| contém `application/json` **e não** `text/html` | JSON |
| ambos / `*/*` / ausente | **JSON** (default — preserva contrato literal PRD AC4) |

Handler global aplica a mesma regra. Em HTML, body `{code, message, hint}` vira `{{ code }}`, `{{ message }}`, `{{ hint }}` no partial.

### Error Catalog

[Source: arch §10.2]

```python
ERROR_CATALOG: dict[str, tuple[str, str]] = {
    "INVALID_INPUT":       ("Invalid request payload.",
                            "Provide exactly one of: url, file, or local_path."),
    "UPLOAD_TOO_LARGE":    ("Upload exceeds size limit.",
                            "Reduce file size or adjust MAX_UPLOAD_MB in .env."),
    "SOURCE_FETCH_FAILED": ("Failed to fetch source.",
                            "Verify the URL is publicly accessible and returns HTTP 200."),
    "CONVERSION_FAILED":   ("Docling conversion failed.",
                            "Source may be corrupted, empty, or a scanned PDF (OCR not supported in v1)."),
    "REQUEST_TIMEOUT":     ("Extraction exceeded timeout.",
                            "Try a smaller source or increase REQUEST_TIMEOUT_SECONDS in .env."),
    "INTERNAL_ERROR":      ("An unexpected error occurred.",
                            "Check the server console logs for details."),
}
```

**Invariantes (arch §10.2):**
1. Todo response de erro tem `{status: "error", code, message, hint}`.
2. `code` é sempre um dos 6 valores do catálogo — testar isso em `test_errors.py`.
3. `hint` nunca null/omitido.
4. HTML e JSON compartilham o mesmo dict.

### Lifespan & Singletons

[Source: arch §2.2 R2, §3.4]

- `DocumentConverter()` instanciado **uma vez** no startup. Primeira execução baixa ~600MB de modelos (R2) — aceitar, documentar em Story 1.6 README.
- `httpx.AsyncClient` também lifespan-scoped (fechar em shutdown).

```python
from contextlib import asynccontextmanager

@asynccontextmanager
async def lifespan(app: FastAPI):
    app.state.converter = DocumentConverter()
    app.state.http_client = make_http_client()
    app.state.templates = Jinja2Templates(directory="frontend/templates")
    yield
    await app.state.http_client.aclose()

app = FastAPI(lifespan=lifespan)
```

### CPU-Bound Execution

[Source: arch §2.2 R1]

Docling é síncrono CPU-bound. Sem `asyncio.to_thread`, o `/health` trava durante uma conversão. Todo o pipeline fica em threadpool via:

```python
result = await asyncio.wait_for(
    asyncio.to_thread(run_pipeline_sync, ...),
    timeout=settings.request_timeout_seconds,
)
```

`run_pipeline_sync` orquestra extract/build/save. Observação: `extract` é `async` (Story 1.2 §6.3) por causa de `httpx`; ajustar a arquitetura criando um wrapper que chame `asyncio.run` internamente ou mover `httpx` calls para fora do `to_thread`. Padrão recomendado:

```python
async def run_pipeline(...) -> SaveResult:
    er: ExtractionResult = await extract(..., http_client=..., converter=...)
    # build + save são puros → to_thread
    def _persist() -> SaveResult:
        fm = build(er.metadata, er.source, now=datetime.now(tz=...))
        return save(fm, er.markdown, er.metadata, er.source, settings.output_dir)
    return await asyncio.to_thread(_persist)
```

A chamada ao Docling (dentro de `extract`) **dentro** do `_run_docling` já deve usar `to_thread`. Opcional: mover essa responsabilidade para `extract` encapsular o `to_thread` (arch §2.1 A4 sugere "no endpoint" — mas o contrato resulta mais limpo se `extract` encapsular e o endpoint apenas `await extract`).

### `local_path` Validation

[Source: arch §2.2 R4 + §11 Security]

Validar no endpoint (não delegar ao service):
1. `Path(local_path).is_file()`
2. Extensão `.pdf`
3. Magic bytes `%PDF-` (ler primeiros 5 bytes)

Path traversal é irrelevante (single-user local, NFR7). Validação é só contra typos óbvios.

### File Size Check

[Source: PRD §6 Story 1.4 AC3]

Para `file` (upload): checar `file.size` (fornecido pelo `UploadFile`) **antes** de ler o conteúdo. FastAPI/Starlette carrega o upload em memória ou spool-file — mesmo assim, validar cedo evita trabalho.

Para `local_path`: `Path(local_path).stat().st_size` > limite → `UploadTooLargeError`.

### File Locations

[Source: arch §9]

| Arquivo | Status nesta story |
|---------|---------------------|
| `backend/main.py` | Atualizar: lifespan, `POST /extract`, mounts |
| `backend/errors.py` | Atualizar: `ERROR_CATALOG`, subclasses restantes, handlers, `register_handlers` |
| `frontend/templates/partials/result_success.html` | Novo (placeholder minimalista — Story 1.5 reestiliza) |
| `frontend/templates/partials/result_error.html` | Novo (idem) |
| `frontend/templates/index.html` | Novo se ainda não existe; pode ser stub — Story 1.5 finaliza |
| `tests/test_endpoints.py` | Novo |
| `tests/test_errors.py` | Novo (cobrir handler + catalog invariantes) |
| `tests/conftest.py` | Atualizar: `test_client`, `override_settings`, `mock_docling` |

### Boundary Rules (reforço)

[Source: arch §10.3]

- Endpoint **sem** `try/except`. Toda tradução via handler global.
- Handler não expõe stack trace no body — apenas `code/message/hint`. Log interno sim.

### Partials Jinja (placeholders mínimos nesta story)

Esta story cria apenas o markup mínimo funcional para os testes AC9 passarem. Story 1.5 aplica o design system (arch frontend-spec §5.11, §5.12).

**`result_success.html` (placeholder):**
```html
<div id="result" class="result-success">
  <p>OK: {{ result.filename }}</p>
  <code>{{ result.output_path }}</code>
</div>
```

**`result_error.html` (placeholder):**
```html
<div id="result" class="result-error">
  <strong>{{ code }}</strong>
  <p>{{ message }}</p>
  <p>{{ hint }}</p>
</div>
```

### Testing

[Source: arch §12.1]

- **Test files:** `tests/test_endpoints.py`, `tests/test_errors.py`
- **Framework:** `pytest` + `pytest-asyncio` + `fastapi.testclient.TestClient`
- **Fixtures:**
  - `test_client` com `app.dependency_overrides[get_settings]` apontando `output_dir` para `tmp_path`
  - `mock_docling_result` — evita chamada real ao Docling
  - `mock_http_transport` — evita rede real em URLs
- **Cobertura obrigatória:** 4 sucessos + 7 falhas + 4 content negotiation = **15 casos**
- **Performance:** <15s para esse arquivo (mocks fazem todo o trabalho pesado)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-18 | 1.0 | Draft inicial criado a partir do Epic 1 / PRD v1.3 (inclui AC9 content negotiation). | River (@sm) |
| 2026-04-19 | 1.1 | Validação PO — verdict GO (8.5/10), Draft → Ready. 2 Should-Fix (não-bloqueantes): (a) ambiguidade na estratégia de threading em Task 3 — `asyncio.to_thread(run_pipeline, ...)` é incompatível com `run_pipeline` async como apresentado em Dev Notes §CPU-Bound; escolher um padrão canônico (envolver `_run_docling` com `to_thread` *dentro* de `extract`, OU criar `run_pipeline_sync` que usa `asyncio.run`); (b) `tests/test_errors.py` é listado em File Locations mas não tem task explícita — adicionar Task 6 cobrindo invariantes do handler (code ∈ 6 catálogo, `hint` sempre presente, HTML/JSON compartilham dict). 2 Nice-to-Have: (c) `quality_gate_tools` inclui `coderabbit` mas CodeRabbit está disabled — remover ou habilitar; (d) Task 3 `datetime.now(tz=...)` deixa tz em branco — especificar `UTC` para casar com `file_writer.save()` default. | Pax (@po) |
| 2026-04-19 | 1.2 | Implementação concluída — InProgress → Ready for Review. Endereçou ambos Should-Fix do PO: (a) canonizado padrão "threadpool inside `extract`" — `backend/docling_service.extract` agora chama `_run_docling` via `asyncio.to_thread` nos três branches (URL/upload/local_path); endpoint usa `await asyncio.wait_for(_run_pipeline(...), timeout=...)` com `_run_pipeline` async encadeando `await extract(...)` + `asyncio.to_thread(_persist)` para build+save; (b) `tests/test_errors.py` criado cobrindo as 4 invariantes do catálogo. Também aplicado Nice-to-Have (d): `run_pipeline` usa `datetime.now(tz=UTC)` explícito. Task 4 (GET /) deliberadamente pulada — story autoriza diferir para Story 1.5. Testes: 70 passed (28 herdados + 42 novos entre endpoints/errors), ruff clean. | Dex (@dev) |
| 2026-04-19 | 1.3 | Quality gate PASS (verdict PASS) — Ready for Review → Done. Empirical evidence: `pytest` 70 passed / `ruff` clean / zero regressão nos 28 testes herdados. AC1-AC9 rastreados com evidência linha-por-linha; invariantes R1, R2, §10.2, §10.3, §11 verificados; ambos Should-Fix do PO (S-a threading, S-b test_errors.py) fechados; Nice-to-Have (c) resolvida removendo `coderabbit` de `quality_gate_tools` para refletir o estado real de `core-config.yaml`. Observações não-bloqueantes registradas em QA Results (validação duplicada defensiva de `local_path`, whitespace-URL via caminho acidental, thread leak cosmético no teste de timeout). | Aria (@architect) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m] (Dex / @dev)

### Debug Log References

- `pytest -q` (full suite) → 70 passed, ~15s (28 herdados + 19 endpoints + 23 errors).
- `ruff check backend tests` → All checks passed (após corrigir B008 com `# noqa` nos defaults FastAPI, E501 quebrando a lista de campos, UP041 trocando `asyncio.TimeoutError` por `TimeoutError`, e I001 ordenando imports).
- Ajustes menores ao aplicar o padrão canônico de threading:
  - `backend/docling_service.py`: adicionado `import asyncio` e envelopado os 3 sites de chamada de `_run_docling` em `await asyncio.to_thread(...)` — mantém contrato público da função inalterado (ainda async, ainda injetando `http_client` + `converter`) e preserva comportamento dos 28 testes de Story 1.2.
  - `backend/errors.py`: função helper renomeada de `_prefers_html` → `prefers_html` (sem underscore) porque `main.py` também precisa consultá-la ao decidir o formato de sucesso; evita acoplar o endpoint a um símbolo privado.
  - Assinatura `TemplateResponse(request, name, context, ...)` (Starlette 0.36+) usada em vez do formato legado com `{"request": request}` dentro do context, para evitar a `DeprecationWarning` do Starlette 0.48 presente no venv.

### Completion Notes List

1. **Threading canônico (PO Should-Fix a):** adotado o padrão "threadpool dentro de `extract`". `docling_service.extract` encapsula `_run_docling` com `asyncio.to_thread` — o endpoint permanece um async straightforward com `await extract(...)` + `asyncio.to_thread(_persist)` no builder+writer, tudo envolto por `asyncio.wait_for(..., timeout=settings.request_timeout_seconds)`. Não foi criado `run_pipeline_sync`; a alternativa é menos idiomática e exigiria `asyncio.run` dentro de `to_thread`.
2. **Test de timeout (AC6):** implementado com `_run_docling` dormindo 2s + `Settings.request_timeout_seconds=1`. `asyncio.wait_for` cancela o await; thread de background termina depois mas seu resultado é descartado — aceitável para single-user local.
3. **Content negotiation (AC9):** helper `prefers_html` cobre a tabela de precedência do arch §7.3 (ausente/`*/*`/ambos → JSON; só `text/html` → HTML). Testado via 4 cenários em `test_endpoints.py` + 7 casos parametrizados em `test_errors.py`.
4. **Handler global (arch §10.3):** endpoint não tem `try/except` de tradução; único `try/except TimeoutError` apenas re-lança como `RequestTimeoutError` do domínio — handler global produz a Response. `unexpected_handler` promove qualquer `Exception` para `INTERNAL_ERROR` (500) e logga via `logging.getLogger("docling_extractor.errors")`.
5. **Partials Jinja:** criados em `frontend/templates/partials/` com markup mínimo funcional (classes `result-success` / `result-error` + os campos obrigatórios do contrato). Story 1.5 aplicará o design system completo.
6. **GET / (Task 4):** deliberadamente pulado; a própria story autoriza diferir para Story 1.5 ("Opcional: criar stub agora ou deixar para Story 1.5").
7. **Lifespan safety em testes:** `tests/conftest.py::test_client` monkey-patcha `backend.main.DocumentConverter` para evitar o download de ~600MB de modelos Docling e instala um `httpx.AsyncClient` com `MockTransport` 404-por-default. Testes que precisam de URL configuram o handler via `install_http_client(handler)`.
8. **CodeRabbit (PO Nice-to-Have c):** permanece *disabled* em `core-config.yaml` — nenhuma rodada de self-healing foi disparada. Reconciliar `quality_gate_tools` com o estado real fica para o @architect no QA gate (remover `coderabbit` da lista OU habilitar globalmente).

### File List

**Created:**
- `frontend/templates/partials/result_success.html`
- `frontend/templates/partials/result_error.html`
- `tests/test_endpoints.py`
- `tests/test_errors.py`

**Modified:**
- `backend/main.py` — lifespan (DocumentConverter, http_client, Jinja2Templates), `/static` mount, global handlers wiring, `POST /extract` endpoint com validação, content negotiation e pipeline orchestrator (`_run_pipeline` + `_NamedBytesIO` + `_validate_local_path`).
- `backend/errors.py` — subclasses adicionais (`UploadTooLargeError`, `RequestTimeoutError`), `ERROR_CATALOG` (6 entradas), `prefers_html`, `handler`, `unexpected_handler`, `register_handlers`.
- `backend/docling_service.py` — `_run_docling` agora invocado via `asyncio.to_thread` nos 3 branches de `extract` (fecha S1 do PO).
- `tests/conftest.py` — `test_settings`, `test_client` (com lifespan mockado), `install_http_client`, `set_run_docling`.

**Status:** `Ready for Review`  → aguarda @architect para quality gate (pytest, httpx, ruff, coderabbit per story metadata).

## QA Results

### Gate: PASS — Architectural Review by @architect (Aria)

**Date:** 2026-04-19
**Reviewer role per story metadata:** `quality_gate: "@architect"` (overrides default @qa assignment)
**Scope:** Full architectural review against the 9 ACs, AIOX arch §2.2, §7, §10, §11, §12.1, plus PO v1.1 Should-Fix closure.

#### Empirical verification

| Check | Command | Result |
|---|---|---|
| Full test suite | `pytest -q` | **70 passed** in 9.34s |
| Story 1.4 tests | `pytest tests/test_endpoints.py tests/test_errors.py` | **42 passed** in 8.87s |
| Lint | `ruff check backend tests` | **All checks passed** |
| Regression — 28 inherited tests (Story 1.1/1.2/1.3) | included in full suite | **0 regressions** |

#### AC-by-AC traceability

| AC | Evidence | Verdict |
|---|---|---|
| AC1 — 3 mutually exclusive fields | `backend/main.py:128-130` (`Form(None)`/`File(None)`/`Form(None)`); `backend/main.py:134-143` (tri-field collector) | ✅ PASS |
| AC2 — Validation + `local_path` guards | `backend/main.py:139-143` (count==1 check); `backend/main.py:83-97` `_validate_local_path` (is_file + `.pdf` + `%PDF-` magic) | ✅ PASS |
| AC3 — 413 before conversion | `backend/main.py:151-159` (two-layer: `file.size` + `len(data)`) | ✅ PASS |
| AC4 — `extract → build → save` + JSON envelope | `backend/main.py:100-122` `_run_pipeline`; `backend/main.py:194-201` success JSON | ✅ PASS |
| AC5 — Typed→HTTP mapping | `backend/errors.py:30-52` subclasses w/ `http_status`; `backend/errors.py:129-133` `register_handlers` | ✅ PASS |
| AC6 — Global timeout | `backend/main.py:172-185` `asyncio.wait_for` + `TimeoutError → RequestTimeoutError` | ✅ PASS |
| AC7/AC8 — 15 integration cases + file content verification | `tests/test_endpoints.py` (4 success w/ `_assert_file_has_frontmatter_and_body` + 7 failures + 4 content-neg + extras) | ✅ PASS |
| AC9 — Content negotiation | `backend/errors.py:83-93` `prefers_html`; endpoint success branch *and* global handler honor it; 7 parametrized precedence tests in `tests/test_errors.py:156-168` | ✅ PASS |

#### Architectural invariant compliance

- **R1 (event-loop responsiveness)** — `docling_service.extract` wraps `_run_docling` in `asyncio.to_thread` at all 3 branches; build+save go to `asyncio.to_thread(_persist)` in `_run_pipeline`; `/health` remains responsive during conversion. ✅
- **R2 (lifespan-scoped singletons)** — `DocumentConverter`, `httpx.AsyncClient`, `Jinja2Templates` created once in `lifespan`; shutdown closes the http client. ✅
- **§10.3 boundary (no `try/except` in endpoint)** — only the `TimeoutError` re-raise survives, which is pure domain translation (no Response produced). Everything else flows through `register_handlers`. ✅
- **§10.2 error-catalog invariants** — 6 codes, `hint` never empty, HTML/JSON share the same dict (`test_html_and_json_share_the_same_payload`), unknown exceptions promoted to `INTERNAL_ERROR` (`test_untyped_exception_promoted_to_internal_error`). ✅
- **§11 security** — Jinja2 HTML autoescape (default); no stack-trace leakage in body (`logger.exception` internal only); size gate before read; path traversal explicitly out-of-scope per NFR7. ✅

#### PO v1.1 Should-Fix closure

| PO concern | Resolution | Verified |
|---|---|---|
| Threading ambiguity (S-a) | Canonicalized "threadpool inside `extract`" — endpoint is straightforward `await extract` + `asyncio.to_thread(_persist)` + `asyncio.wait_for` | ✅ |
| Missing `test_errors.py` task (S-b) | Task 6 added; 23 tests covering the 4 catalog invariants + precedence table | ✅ |
| `datetime.now(tz=UTC)` explicit (NtH-d) | `backend/main.py:119` uses `datetime.now(tz=UTC)` | ✅ |
| `coderabbit` in `quality_gate_tools` drift (NtH-c) | Removed from story header in this same gate commit — now `[pytest, httpx, ruff]`, matching `core-config.yaml` disabled state | ✅ |

#### Non-blocking observations (recorded for future cleanup, not gating)

1. **Defense-in-depth validation on `local_path`** — `backend/main.py:83-97` and `backend/docling_service.py:220-233` both validate existence/extension/magic bytes. Intentional per arch §2.2 R4; worth a one-liner in Dev Notes to discourage a future "DRY cleanup" regression.
2. **Whitespace-only URL** (`"   "`) — endpoint validator treats non-empty strings as provided (truthy), so rejection happens deeper inside `extract` as `File not found` → 400. `test_extract_rejects_empty_string_url["   "]` passes but via an unintentional branch. Cosmetic — `if value and value.strip()` would make the intent explicit.
3. **Timeout test thread leak** — `test_extract_timeout_returns_504` leaves a `time.sleep(2)` thread outliving the test by ~1s. Documented in Completion Note #2; acceptable for local single-user, not a CI flake risk.

#### Verdict

**✅ PASS** — Story 1.4 is architecturally sound. All 9 ACs met with line-level evidence; all architectural invariants (R1, R2, §10.2, §10.3, §11) upheld; both PO Should-Fix items closed; zero regressions across 28 inherited tests. Status flipped Ready for Review → Done; `quality_gate_tools` reconciled to match `core-config.yaml`. Hand to **@devops** for push/PR when desired.

— Aria (@architect), arquitetando o futuro 🏗️
