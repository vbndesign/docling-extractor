# Story 1.3: Frontmatter Builder & File Writer

## Status

Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [pytest, ruff, coderabbit]
```

## Story

**As** the backend,
**I want** a deterministic component that takes (markdown_body, metadata, source) and produces a final `.md` file written to the output dir with the correct filename and frontmatter,
**so that** the output always respects the "minimum frontmatter" rule and naming convention without ambiguity.

## Acceptance Criteria

1. `backend/frontmatter.py` expõe função `build(metadata, source, now) -> str` que retorna o YAML frontmatter como string (delimitado por `---`).
2. Os seguintes campos DEVEM sempre estar presentes no YAML gerado: `note_type: literature_document`, `created` (YYYY-MM-DD), `extracted_at` (ISO 8601 com timezone), `location` (URL ou nome do arquivo original), `generated_by: docling`.
3. Os seguintes campos DEVEM estar presentes **somente se** vieram preenchidos no dict `metadata`: `source_title`, `author`, `year`. Se ausentes, omitir completamente — nunca incluir como string vazia, null ou placeholder.
4. Os campos `title`, `source_type`, `domain`, `concepts_extracted` **nunca** são gerados pela tool.
5. Valores com caracteres especiais (aspas, colons, caminhos Windows com barras invertidas) são corretamente escapados no YAML usando `yaml.safe_dump` ou equivalente.
6. `backend/file_writer.py` expõe função `save(frontmatter_str, markdown_body, metadata, source, output_dir) -> Path` que: (a) concatena `frontmatter_str + "\n" + markdown_body`, (b) deriva o nome do arquivo pela ordem de prioridade definida em FR7, (c) aplica slug kebab-case ASCII, (d) escreve no `output_dir`, (e) retorna o Path absoluto do arquivo criado.
7. Se um arquivo com o mesmo nome já existir no output, o writer adiciona sufixo numérico incremental (`-2`, `-3`) em vez de sobrescrever silenciosamente.
8. Testes unitários em `tests/test_frontmatter.py` cobrem: todos os campos condicionais presentes, todos ausentes, combinações mistas, e verificação de que campos proibidos nunca aparecem.
9. Testes do file_writer cobrem as 3 origens de nome (title, filename, URL slug) e o caso de colisão com sufixo incremental.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [ ] Task 1 — Implementar `backend/frontmatter.py::build` (AC: 1, 2, 3, 4, 5)
  - [ ] Assinatura: `def build(metadata: ExtractedMetadata, source: SourceDescriptor, now: datetime) -> str`
  - [ ] Campos sempre presentes: `note_type`, `created` (YYYY-MM-DD), `extracted_at` (ISO 8601 com tz), `location`, `generated_by`
  - [ ] Campos condicionais (incluir se `not None`): `source_title`, `author`, `year`
  - [ ] Usar `yaml.safe_dump(sort_keys=False, allow_unicode=True, default_flow_style=False)` para escape correto (AC5)
  - [ ] Retorno: string no formato `---\n{yaml}---` (com `---` no começo e fim)
  - [ ] Verificar que campos proibidos (`title`, `source_type`, `domain`, `concepts_extracted`) **nunca** aparecem no output
- [ ] Task 2 — Implementar `backend/file_writer.py::save` (AC: 6, 7)
  - [ ] Assinatura: `def save(frontmatter_str, markdown_body, metadata, source, output_dir) -> SaveResult`
  - [ ] Adicionar dataclass `SaveResult(output_path: Path, filename: str)` em `backend/models.py` (se ainda não existe)
  - [ ] Derivar filename por prioridade (FR7):
    1. `metadata.source_title` (se presente)
    2. `source.original_filename` stem (se `source.kind` em `{pdf_upload, pdf_local_path}`)
    3. Slug da URL (`source.location`) — parte do path final
  - [ ] Aplicar `python_slugify.slugify(raw, separator="-", lowercase=True, max_length=200)` (R5)
  - [ ] Fallback R9: se slug vazio → `document-{timestamp}`
  - [ ] Extensão fixa `.md`
  - [ ] Concatenar conteúdo: `frontmatter_str + "\n" + markdown_body`
  - [ ] Escrever atomicamente com `Path.open('x', encoding='utf-8')` (R6 — fail if exists)
  - [ ] Collision loop (AC7): se `FileExistsError`, tentar `-2`, `-3`, ... até conseguir
  - [ ] Retornar `SaveResult(output_path=abs_path, filename=basename)`
- [ ] Task 3 — Escrever `tests/test_frontmatter.py` (AC: 8)
  - [ ] `test_build_all_required_fields_present` — nenhum opcional preenchido
  - [ ] `test_build_all_optional_fields_present` — todos preenchidos
  - [ ] `test_build_mixed_optional_fields` — alguns preenchidos, outros None
  - [ ] `test_build_omits_missing_fields` — `None` NÃO aparece como `null`, `""` ou placeholder
  - [ ] `test_build_never_includes_forbidden_fields` — `title`, `source_type`, `domain`, `concepts_extracted` ausentes
  - [ ] `test_build_escapes_special_chars` — título com aspas/colons/backslashes é escapado corretamente (parseável por `yaml.safe_load`)
  - [ ] `test_build_uses_local_date_for_created_and_iso8601_for_extracted_at`
  - [ ] `test_build_wraps_in_triple_dashes` — formato `---\n...\n---`
- [ ] Task 4 — Escrever `tests/test_file_writer.py` (AC: 9)
  - [ ] `test_save_uses_title_as_filename` — metadata tem `source_title`
  - [ ] `test_save_uses_original_filename_when_no_title` — upload de PDF sem title metadata
  - [ ] `test_save_uses_url_slug_when_no_title_and_no_filename` — URL com path `/artigos/meu-post`
  - [ ] `test_save_applies_kebab_case_slug` — caracteres especiais viram slug ASCII
  - [ ] `test_save_collision_appends_incremental_suffix` — criar mesmo nome 3x e verificar `-2`, `-3`
  - [ ] `test_save_long_title_truncated_to_200_chars` (R5)
  - [ ] `test_save_empty_slug_fallback` (R9) — título só com caracteres CJK gera `document-{timestamp}.md`
  - [ ] `test_save_returns_absolute_path_and_filename`
  - [ ] Usar `tmp_path` do pytest como `output_dir`

## Dev Notes

### Previous Story Insights

Story 1.2 criou `backend/models.py` (`ExtractedMetadata`, `SourceDescriptor`, `ExtractionResult`), `backend/errors.py` (parcial), `backend/docling_service.py::extract`. Esta story completa a pipeline pura (sem I/O de rede, sem Docling) adicionando `frontmatter.py` e `file_writer.py`.

### SourceDescriptor (herdado de Story 1.2)

[Source: arch §5.1]

```python
@dataclass(frozen=True)
class SourceDescriptor:
    kind: Literal["url_html", "url_pdf", "pdf_upload", "pdf_local_path"]
    location: str                           # URL ou filename ou path absoluto
    original_filename: Optional[str] = None # apenas p/ pdf_upload, pdf_local_path
```

### Frontmatter Schema

[Source: arch §5.2, PRD §6 Story 1.3 AC2/3/4]

**Sempre presente:**
```yaml
note_type: literature_document
created: 2026-04-18                     # YYYY-MM-DD, data local
extracted_at: 2026-04-18T14:32:11-03:00 # ISO 8601 com tz
location: https://example.com/article   # URL ou filename (vem de source.location)
generated_by: docling
```

**Se detectado (senão omitir):**
```yaml
source_title: "Example Title"
author: "Jane Doe"
year: 2024
```

**Proibido gerar:** `title`, `source_type`, `domain`, `concepts_extracted` (FR5).

### Interface Contract — `build`

[Source: arch §6.3]

```python
def build(
    metadata: ExtractedMetadata,
    source: SourceDescriptor,
    now: datetime,
) -> str:
    """Retorna string '---\\n<yaml>---' pronta para concatenar ao markdown."""
```

- `now` é parâmetro (não `datetime.now()` interno) — determinístico e testável.
- `now.date().isoformat()` → campo `created`.
- `now.isoformat()` (com tz) → campo `extracted_at`. Garantir que `now` chegue tz-aware (caller responsável em Story 1.4).

### Filename Derivation (FR7 + arch §5.1)

**Ordem de prioridade:**
1. `metadata.source_title` (se `not None` e não vazio após strip)
2. `source.original_filename` (stem, sem extensão) — quando `source.kind in {"pdf_upload", "pdf_local_path"}`
3. Slug derivado da URL (`source.location`): pegar último segmento do path; se vazio, usar host.

**Transformação:**
- `python_slugify.slugify(raw, separator="-", lowercase=True, max_length=200)`
- Fallback R9 (slug vazio): `f"document-{int(now.timestamp())}"` (Story 1.4 passará `now`; nesta story, basta aceitar um parâmetro opcional `now` no writer ou gerar via `datetime.now()` no fallback — preferir receber `now` para testabilidade).

### Interface Contract — `save`

[Source: arch §6.3 + §5.1]

```python
def save(
    frontmatter_str: str,
    markdown_body: str,
    metadata: ExtractedMetadata,
    source: SourceDescriptor,
    output_dir: Path,
) -> SaveResult: ...
```

### SaveResult dataclass

[Source: arch §5.1]

```python
@dataclass(frozen=True)
class SaveResult:
    output_path: Path  # absoluto
    filename: str      # basename
```

Adicionar a `backend/models.py` se ainda não está lá (Story 1.2 pode ter deixado isso para cá — verificar antes de duplicar).

### Collision Handling

[Source: arch §2.2 R6 + PRD §6 Story 1.3 AC7]

Usar `Path.open('x', encoding='utf-8')` (mode `x` = exclusive-create, `FileExistsError` se já existir). Loop:

```python
base = slug
ext = ".md"
n = 1
while True:
    candidate = output_dir / (f"{base}{ext}" if n == 1 else f"{base}-{n}{ext}")
    try:
        with candidate.open("x", encoding="utf-8") as f:
            f.write(content)
        return SaveResult(output_path=candidate.resolve(), filename=candidate.name)
    except FileExistsError:
        n += 1
```

Isso é TOCTOU-safe (arch §2.2 R6).

### Edge Cases

[Source: arch §2.2 R5, R9]

- **Slug > 200 chars (R5):** truncar ANTES do sufixo de colisão e extensão. Windows NTFS trava em nomes >255 chars.
- **Slug vazio (R9):** fallback `document-{timestamp}`.

### Boundary Rules

[Source: arch §10.3]

- `file_writer` levanta `DoclingExtractorError` (ou subclasse `InternalError`) só se operação de disco falhar por motivo não-colisão. Nunca `HTTPException`.
- `frontmatter` é função pura — não levanta exceptions de domínio; erros de serialização YAML são bugs que devem aparecer nos testes.

### File Locations

[Source: arch §9]

| Arquivo | Status nesta story |
|---------|---------------------|
| `backend/frontmatter.py` | Novo |
| `backend/file_writer.py` | Novo |
| `backend/models.py` | Atualizar (adicionar `SaveResult` se faltava) |
| `tests/test_frontmatter.py` | Novo |
| `tests/test_file_writer.py` | Novo |

### Testing

[Source: arch §12.1 + §12.2]

- **Test files:** `tests/test_frontmatter.py`, `tests/test_file_writer.py`
- **Framework:** `pytest`
- **Fixtures:**
  - `tmp_path` (builtin pytest) para `output_dir`
  - Reusar fixtures de `conftest.py` se aplicável
  - Para testar escape YAML: `yaml.safe_load(yaml_output)` deve reparsear o bloco e bater com o input
- **Determinismo:** passar `now` como parâmetro em todos os testes (sem `datetime.now()` real)
- **Performance:** ambos os arquivos ficam <5s combinados

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-18 | 1.0 | Draft inicial criado a partir do Epic 1 / PRD v1.3. | River (@sm) |

## Dev Agent Record

### Agent Model Used

_(preenchido pelo @dev)_

### Debug Log References

_(preenchido pelo @dev)_

### Completion Notes List

_(preenchido pelo @dev)_

### File List

_(preenchido pelo @dev)_

## QA Results

_(preenchido pelo @qa)_
