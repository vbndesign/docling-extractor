# Story 1.1: Project Scaffolding & Configuration

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [pytest, ruff, coderabbit, uvicorn-smoke]
```

## Story

**As a** solo developer setting up the project,
**I want** a minimal FastAPI application skeleton with config, folder structure, and a health endpoint,
**so that** I have a reliable foundation to iterate on without fighting plumbing.

## Acceptance Criteria

1. Repositório inicializado com a estrutura de pastas definida em Technical Assumptions (`backend/`, `frontend/`, `tests/`, `output/`).
2. `pyproject.toml` declara Python 3.11+ e as dependências diretas **com limites superiores de versão** (S2): `fastapi>=0.110,<0.120`, `uvicorn[standard]>=0.27,<0.31`, `docling>=2.0,<3.0`, `httpx>=0.27,<0.28`, `jinja2>=3.1,<4.0`, `python-multipart>=0.0.9,<0.1`, `pyyaml>=6.0,<7.0`, `python-slugify>=8.0,<9.0`, mais dev-deps `pytest>=8.0,<9.0`, `pytest-asyncio`, `ruff>=0.4,<0.5`. Os ranges permitem patches sem drift de major/minor.
3. `backend/config.py` define constantes/env vars para `OUTPUT_DIR` (default `./output`), `MAX_UPLOAD_MB` (default 50), `REQUEST_TIMEOUT_SECONDS` (default 60).
4. `backend/main.py` expõe app FastAPI com endpoint `GET /health` retornando `{"status": "ok"}` e status 200.
5. `uvicorn backend.main:app` sobe sem erros e o `/health` responde corretamente em browser/curl.
6. `.gitignore` exclui `output/`, `__pycache__/`, `.venv/`, `.env`.
7. `.env.example` documenta as variáveis de configuração disponíveis.
8. `README.md` inicial cobre: pré-requisitos (Python 3.11+), instalação, como rodar o servidor, como checar o `/health`.
9. Repositório inicializado com `git init` + commit inicial incluindo `.gitignore`, `.env.example` e toda a estrutura de pastas base (S1). Mensagem do commit segue conventional commits (ex: `chore: initial project scaffolding`).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1 — Criar a árvore de pastas do projeto (AC: 1)
  - [x] Criar `backend/__init__.py`, `frontend/templates/`, `frontend/static/`, `tests/fixtures/`, `output/`
  - [x] Conferir contra a source tree em arch §9
- [x] Task 2 — Escrever `pyproject.toml` com dependências pinadas (AC: 2)
  - [x] Declarar `[project]` com `requires-python = ">=3.11"`
  - [x] Adicionar dependências runtime com ranges `>=X,<Y` conforme AC2
  - [x] Adicionar `[project.optional-dependencies].dev` com pytest, pytest-asyncio, ruff
  - [x] Validar instalação: `uv pip install -e ".[dev]"` (ou `pip install -e ".[dev]"`)
- [x] Task 3 — Implementar `backend/config.py` (AC: 3)
  - [x] Classe `Settings` lendo `OUTPUT_DIR`, `MAX_UPLOAD_MB`, `REQUEST_TIMEOUT_SECONDS` via `os.environ` com defaults
  - [x] Exportar `get_settings()` compatível com `fastapi.Depends` (arch §3.4 — DI pattern)
- [x] Task 4 — Implementar `backend/main.py` mínimo (AC: 4, 5)
  - [x] Criar app FastAPI + rota `GET /health` → `{"status": "ok"}`
  - [x] Confirmar boot: `uvicorn backend.main:app --host 127.0.0.1 --port 8000`
  - [x] Validar `curl http://127.0.0.1:8000/health` retorna 200
- [x] Task 5 — Criar `.gitignore`, `.env.example`, `README.md` iniciais (AC: 6, 7, 8)
  - [x] `.gitignore`: `output/`, `__pycache__/`, `.venv/`, `.env`, `*.pyc`
  - [x] `.env.example`: documentar `OUTPUT_DIR`, `MAX_UPLOAD_MB`, `REQUEST_TIMEOUT_SECONDS`
  - [x] `README.md`: pré-requisitos, instalação, como rodar, como checar `/health`
- [x] Task 6 — Git init + commit inicial (AC: 9)
  - [x] `git init` se ainda não for repositório
  - [x] `git add .` + `git commit -m "chore: initial project scaffolding"`
  - [x] Verificar que `output/` não entra no commit (gitignored)
- [x] Task 7 — Smoke test final
  - [x] Subir server, hit `/health`, verificar log limpo sem warnings

## Dev Notes

### Relevant Source Tree

Fonte: arch §9.

```
docling-extractor/
├── backend/
│   ├── __init__.py
│   ├── main.py                 # app factory, lifespan, routes, static+templates mounts
│   ├── config.py               # Settings (env-driven), get_settings() dependency
│   ├── errors.py               # [Story 1.2+] exception hierarchy + handlers
│   ├── models.py               # [Story 1.2+] dataclasses
│   ├── docling_service.py      # [Story 1.2]
│   ├── frontmatter.py          # [Story 1.3]
│   └── file_writer.py          # [Story 1.3]
├── frontend/
│   ├── templates/              # [Story 1.5]
│   └── static/                 # [Story 1.5]
├── tests/
│   ├── __init__.py
│   ├── conftest.py             # [Story 1.2+]
│   └── fixtures/               # [Story 1.2+]
├── output/                     # gitignored
├── pyproject.toml
├── .env.example
├── .gitignore
└── README.md
```

Na Story 1.1, apenas os arquivos marcados sem "[Story X.Y]" precisam ter conteúdo real; os demais podem ser placeholders vazios (`__init__.py` ou pasta vazia) — mas a árvore completa deve existir para o commit inicial atender AC1.

### Configuration Contract (backend/config.py)

[Source: arch §3.4 Architectural Patterns — Dependency Injection]

```python
# Shape esperado:
class Settings:
    output_dir: Path        # de env OUTPUT_DIR, default Path("./output")
    max_upload_mb: int      # de env MAX_UPLOAD_MB, default 50
    request_timeout_seconds: int  # de env REQUEST_TIMEOUT_SECONDS, default 60

def get_settings() -> Settings: ...
```

Stories seguintes vão sobrescrever `get_settings` via `app.dependency_overrides` nos testes. Manter a função simples e idempotente.

### Health Endpoint

[Source: PRD §6 Story 1.1 AC4, arch §7.1]

`GET /health` → `{"status": "ok"}` com HTTP 200. Liveness simples. Nenhum check de dependências nesta story (Docling converter só é instanciado em Story 1.4 via lifespan).

### Platform & Binding

[Source: arch §3.2, R8 em §2.2]

- Bind default: `127.0.0.1` (nunca `0.0.0.0` por default — R8).
- Documentar no README como mudar para LAN (Story 1.6 cobrirá expansão).

### Dependências Pinadas (rationale)

[Source: arch §4 Tech Stack + PRD §6 Story 1.1 AC2]

Version ranges `>=X,<Y` evitam drift de major/minor mas liberam patches. Evitar `==` (congela bugs corrigidos) e evitar `>=X` sem teto (quebra silenciosa em major release).

### Git Initial Commit

[Source: PRD §6 Story 1.1 AC9, arch §2.3 S1]

Commit inicial deve conter apenas o esqueleto. Sem código funcional de stories posteriores. Mensagem: conventional commits (`chore: initial project scaffolding`).

### Project Structure Notes

Sem divergências entre PRD §4 e arch §9 para os arquivos-alvo desta story. `models.py` e `errors.py` aparecem em arch §9 (não no PRD §4) — eles serão criados nas Stories 1.2 / 1.3 / 1.4; manter apenas o diretório `backend/` pronto nesta story.

### Testing

[Source: arch §12 Testing Strategy]

- Nenhum teste automatizado é requisitado por Story 1.1 (AC apenas pedem smoke manual via curl/browser).
- Os arquivos de teste (`test_*.py`, `conftest.py`, `fixtures/`) só precisam existir como placeholders vazios — `tests/__init__.py` e `tests/fixtures/` são criados para estrutura, sem fixtures commitadas ainda.
- Stack de teste (`pytest`, `pytest-asyncio`, `TestClient`) vem de AC2 mas só começa a ser usada em Story 1.2+.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-18 | 1.0 | Draft inicial criado a partir do Epic 1 / PRD v1.3. | River (@sm) |
| 2026-04-18 | 1.1 | Validation GO (10/10, readiness 9.5/10). Status Draft → Ready. One advisory (Task 1 should list `tests/__init__.py` explicitly) não bloqueia. | Pax (@po) |
| 2026-04-18 | 1.2 | Implementation complete. All 7 tasks done, all 9 ACs met, smoke test passing. Status Ready → Ready for Review. | Dex (@dev) |
| 2026-04-18 | 1.3 | QA gate PASS (9/9 ACs, all 7 quality checks). Two LOW observations deferred to Stories 1.2/1.3. | Quinn (@qa) |
| 2026-04-18 | 1.4 | Branch pushed to origin. Status Ready for Review → Done. | Gage (@devops) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — `claude-opus-4-7[1m]` — AIOX `@dev` (Dex).

### Debug Log References

- Uvicorn boot log (clean, no warnings):
  ```
  INFO:     Started server process [40500]
  INFO:     Waiting for application startup.
  INFO:     Application startup complete.
  INFO:     Uvicorn running on http://127.0.0.1:8765
  INFO:     127.0.0.1 - "GET /health HTTP/1.1" 200 OK
  ```
- Install validation: `pip install -e ".[dev]"` completed successfully on Python 3.14.0 (`requires-python = ">=3.11"` is satisfied). Docling 2.87.0 + torch 2.11.0 resolved.

### Completion Notes List

- All 9 ACs met. Scaffolding builds, installs cleanly, `/health` returns `{"status":"ok"}` 200 on `127.0.0.1:8000` default bind (R8 honored).
- Existing `.env.example` at repo root is shared with the AIOX framework; docling-specific vars were appended under a clearly delimited section rather than creating a second file, to avoid confusing the single-venv single-`.env` local workflow.
- Existing `.gitignore` already contained Python + `output/` entries from a prior branch setup — verified it satisfies AC6 without further changes.
- Repo was already a git repository (branch `feature/epic-1-docling-extractor`) — `git init` step of AC9 was a no-op; a scaffolding commit on the current feature branch fulfills the intent.
- `output/.gitkeep` was deliberately NOT added — the directory is gitignored and gets auto-created at runtime in later stories.
- `tests/__init__.py` was added explicitly (addresses the @po validation advisory).
- Python 3.14.0 was used locally; the project pins `>=3.11`, so Docling's 3.11/3.12 matrix remains the baseline for later stories.

### File List

**New files:**
- `backend/__init__.py` (empty package marker)
- `backend/config.py` (Settings dataclass + `get_settings()` DI helper)
- `backend/main.py` (FastAPI app + `GET /health`)
- `frontend/templates/.gitkeep`
- `frontend/static/.gitkeep`
- `tests/__init__.py`
- `tests/fixtures/.gitkeep`
- `pyproject.toml`
- `README.md`

**Modified files:**
- `.env.example` (appended docling-extractor configuration block)

**Untracked/runtime (not committed):**
- `.venv/` (gitignored)
- `output/` (gitignored, empty)

## QA Results

**Reviewer:** Quinn (@qa) · **Date:** 2026-04-18 · **Commit:** `e3cb137` · **Verdict:** ✅ **PASS**

**Gate file:** `docs/qa/gates/1.1-project-scaffolding.yml`

### Summary

All 9 acceptance criteria verified against the actual files. Smoke test reproduced independently (uvicorn on `127.0.0.1:8766`, `GET /health` → 200 `{"status":"ok"}`, **zero warnings** at WARN log level). Ruff lint + format clean on `backend/`. OpenAPI surface is exactly `/health` — no accidental endpoints. No gitignored paths are tracked. CodeRabbit is disabled in `core-config.yaml` — manual review only, noted in the gate file.

### Acceptance Criteria — 9/9 met

| AC | Requirement | Result |
|---|---|---|
| 1 | Folder tree per arch §9 | ✅ `backend/`, `frontend/{templates,static}`, `tests/{__init__.py,fixtures/}`, `output/` (gitignored) |
| 2 | `pyproject.toml`, Python 3.11+, deps pinned `>=X,<Y` | ✅ All 8 runtime + 3 dev deps match AC2 ranges exactly; install verified on Py 3.14 |
| 3 | `backend/config.py` — 3 env vars + defaults | ✅ Frozen `Settings` dataclass + `get_settings()` DI; runtime load returns defaults (`./output`, `50`, `60`) |
| 4 | `GET /health` → `{"status":"ok"}` 200 | ✅ Content-Type `application/json`; schema declared in OpenAPI |
| 5 | Uvicorn boots cleanly, `/health` responds | ✅ Clean log (zero WARN+ lines); `/nope` correctly returns 404 |
| 6 | `.gitignore` excludes `output/`, `__pycache__/`, `.venv/`, `.env` | ✅ All four present; `.egg-info/`, `.pytest_cache/`, `.ruff_cache/` added as bonus hardening |
| 7 | `.env.example` documents 3 vars | ✅ Appended section with descriptions + defaults |
| 8 | README: prerequisites / install / run / `/health` | ✅ All four sections; includes LAN-exposure warning |
| 9 | Conventional-commits scaffolding commit | ✅ `e3cb137 chore: initial project scaffolding [Story 1.1]` |

### Quality Gate — 7 checks

- **Code review** — PASS. Minimal, idiomatic. `from __future__ import annotations` + frozen dataclass is the right choice.
- **Unit tests** — N/A by story design (smoke-only per Testing section).
- **Acceptance criteria** — PASS (9/9).
- **Regressions** — PASS (changes are additive only).
- **Performance** — N/A for scaffolding.
- **Security** — PASS. `127.0.0.1` default bind honours arch §2.2 R8; README warns about `--host 0.0.0.0` + auth absence; `.env` gitignored; `.env.example` has no real secrets.
- **Documentation** — PASS. README accurate; Dev Agent Record well populated; Change Log entry added correctly.

### Additional checks performed (beyond the 7)

- `ruff check backend/` → **all checks passed**
- `ruff format --check backend/` → **3 files already formatted**
- `git ls-files | grep -Ei 'output/|.venv/|.egg-info|.env$|__pycache__'` → **no matches**
- `curl /openapi.json` → routes: `['/health']` only
- `from backend.config import get_settings, Settings` → imports + defaults OK

### Observations (non-blocking, carry to later stories)

1. **[LOW | `backend/config.py:22`]** `_load_settings()` currently duplicates `get_settings()` with no behavioural difference. → Inline it now, or give `get_settings()` a real role (e.g., `lru_cache` or a `dependency_overrides` test hook) when Story 1.2 introduces the first `TestClient` test.
2. **[LOW | `backend/config.py:24-26`]** `int(env_value)` has no bounds check; a zero/negative `MAX_UPLOAD_MB` or `REQUEST_TIMEOUT_SECONDS` would be accepted silently. `OUTPUT_DIR` is stored raw — no `~` expansion, no resolution. → Defer to Story 1.2/1.3 — add positive-int validation on `Settings` fields and `.expanduser().resolve()` on `output_dir` where `file_writer` composes paths.

Neither observation blocks this gate.

### Recommendation

Story 1.1 is ready to move to **Done** once `@devops` pushes `feature/epic-1-docling-extractor`. No rework required on this story — carry the two LOW observations into Story 1.2 (Settings validation) and Story 1.3 (output_dir resolution) context rather than reopening 1.1.

— Quinn, guardião da qualidade 🛡️
